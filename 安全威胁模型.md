# cs-qa-platform 一期安全威胁模型

## 1. 范围与假设

范围包括浏览器、Nginx、Go API、Next.js、MySQL、Redis、LLM/SMS/天御/七牛云和 Docker Compose。

假设服务通过公网 HTTPS 访问，一期没有管理员角色；问题、回答、手机号和令牌属于敏感数据；MySQL/Redis 只在 Compose 内网可见。

## 2. 资产与边界

| 资产 | 主要影响 |
|---|---|
| 手机号和历史问答 | 隐私泄露、合规风险 |
| JWT 密钥和刷新令牌 | 账号接管 |
| 外部服务凭证 | 财务损失、数据泄露 |
| LLM 调用额度 | 成本和可用性 |
| 数据库和审核日志 | 内容完整性和事故追踪 |
| 服务器与 Docker | 服务接管 |

信任边界：

1. 浏览器 ↔ Nginx：公网输入、Cookie、Authorization。
2. Nginx ↔ Go/Next.js：反向代理、请求体和超时。
3. Go ↔ MySQL/Redis：内部网络和数据库凭证。
4. Go ↔ 外部服务：公网 API、第三方响应和密钥。
5. LLM 输出 ↔ Markdown/URL 渲染：模型输出视为不可信内容。

## 3. 主要攻击路径

| ID | 攻击路径 | 影响 | 优先级 | 控制 |
|---|---|---|---|---|
| TM-01 | JWT Secret 或刷新令牌泄露 | 账号接管 | 高 | 环境变量、SHA-256、httpOnly/Secure、轮换吊销、禁止日志记录 |
| TM-02 | 绕过验证码限流 | 短信费用和账号滥用 | 高 | 60 秒限频、每日上限、IP 限流、哈希、过期、一次性 |
| TM-03 | 恶意内容进入 Markdown HTML | XSS 和账号接管 | 高 | 禁止原始 HTML、React 安全渲染、URL 协议校验、CSP |
| TM-04 | 修改问题 ID 访问他人数据 | 隐私泄露 | 高 | 服务端按 user_id 授权，越权返回 404/403，集成测试覆盖 |
| TM-05 | 恶意 URL 诱导后端访问内网 | SSRF 和云凭证泄露 | 高 | 阻断环回/私网/元数据地址，限制 DNS、重定向、响应体和超时 |
| TM-06 | 伪造 Cookie 请求 refresh/logout | CSRF | 中 | SameSite=Lax、Origin/Referer、CSRF 自定义请求头、CORS 白名单 |
| TM-07 | LLM/审核服务异常 | 问答失败或合规失效 | 中 | 独立超时、审核默认拒绝、错误脱敏、限流和 request_id |
| TM-08 | Docker/SSH 配置错误 | 数据库暴露和服务接管 | 高 | 只开 22/80/443、SSH 公钥、.env 600、镜像更新、备份演练 |
| TM-09 | 依赖或构建链污染 | 生产代码植入 | 中 | go.sum/package-lock、npm ci、govulncheck、npm audit、依赖审查 |
| TM-10 | 内容消耗 LLM 和抓取资源 | DoS 和成本超支 | 中 | 问题长度 2000、用户/IP 限流、URL 最多 5 个、抓取并发 ≤5 |

## 4. 上线前验证

必须验证 TM-02、TM-03、TM-04、TM-05、TM-06 和 TM-08。运行时监控验证码发送量、提问量、LLM token、5xx/429、SSRF 拦截、审核拒绝、容器内存、磁盘和备份结果。

## 5. 未决事项

正式域名、CORS 白名单、HTTPS 证书和外部服务凭证由上线前运维配置。WAF、图形验证码和集中式日志按公网流量与成本决定。多 Agent、聊天室和新闻推送属于二期，届时需重新评估提示词注入、工具调用和 UGC 审核风险。

