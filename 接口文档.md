# cs-qa-platform 接口文档

| 文档属性 | 内容 |
|---|---|
| 项目 | cs-qa-platform 计算机知识 AI 问答平台 |
| 版本 | v1.0 |
| 日期 | 2026-08-30 |
| 状态 | 一期接口契约 |
| 基础路径 | `/api/v1` |

本文档用于前后端联调。接口设计依据《需求分析文档》《设计方案》《技术栈文档》和《UI 设计文档》。未在一期范围内的功能不新增接口。

---

## 1. 通用约定

### 1.1 请求

- 默认使用 `Content-Type: application/json`。
- 所有时间使用 ISO 8601 格式，例如 `2026-08-30T19:00:00+08:00`。
- ID 使用整数或数字字符串均可，但同一接口版本内必须保持一致；本文示例使用整数。
- 手机号只接受中国大陆手机号格式：11 位、1 开头。
- 问题内容长度为 1～2000 个字符，服务端必须再次校验。
- 请求可携带 `X-Request-ID`；未携带时由服务端生成，并在响应和日志中返回/记录。

### 1.2 鉴权

除特别说明外，受保护接口使用：

```http
Authorization: Bearer <access_token>
```

访问令牌有效期 30 分钟，仅允许前端保存在内存中，禁止写入 `localStorage`、`sessionStorage` 或 URL。

刷新令牌由服务端通过 httpOnly Cookie 下发，前端不能读取其值：

```http
Set-Cookie: refresh_token=<opaque-token>; HttpOnly; Secure; SameSite=Lax; Path=/api/v1/auth
```

本地 HTTP 开发环境可按环境配置关闭 `Secure)，生产必须开启 HTTPS 和 `Secure)。

### 1.3 统一响应包络

成功：

```json
{
  "data": {},
  "request_id": "a1b2c3"
}
```

失败：

```json
{
  "error": {
    "code": "INVALID_SMS_CODE",
    "message": "验证码错误或已过期"
  },
  "request_id": "a1b2c3"
}
```

约定：

- `data` 只出现在成功响应中。
- `error` 只出现在失败响应中。
- 客户端只依赖 `error.code` 做分支，使用 `error.message` 展示提示。
- 服务端不得在响应中返回堆栈、密钥、验证码、令牌或第三方服务原始敏感信息。

### 1.4 HTTP 状态码

| 状态码 | 含义 |
|---|---|
| 200 | 请求成功 |
| 201 | 资源创建成功；如实现统一返回 200，需保持全项目一致 |
| 400 | 参数格式或业务参数不合法 |
| 401 | 未登录、访问令牌无效或刷新 Cookie 无效 |
| 403 | 已登录但无权访问目标资源 |
| 404 | 资源不存在，或为避免越权而隐藏资源 |
| 409 | 当前状态冲突，例如重复发送验证码 |
| 429 | 请求过于频繁 |
| 500 | 服务端内部错误 |
| 502 | LLM、短信、审核等上游服务失败 |
| 503 | 服务未就绪或依赖不可用 |

---

## 2. 接口总览

| 方法 | 路径 | 鉴权 | 说明 |
|---|---|---|---|
| POST | `/auth/sms-code` | 否 | 请求短信验证码 |
| POST | `/auth/login` | 否 | 验证码登录/自动注册 |
| POST | `/auth/refresh` | refresh Cookie | 刷新访问令牌 |
| POST | `/auth/logout` | refresh Cookie | 吊销刷新令牌并清除 Cookie |
| POST | `/questions` | Bearer | 提交问题并同步返回回答 |
| GET | `/questions?page&size` | Bearer | 查询当前用户历史问题 |
| GET | `/questions/:id` | Bearer | 查询当前用户问答详情 |
| GET | `/health` | 否 | 存活检查 |
| GET | `/ready` | 否 | 就绪检查 |

---

## 3. 认证接口

### 3.1 请求短信验证码

```http
POST /api/v1/auth/sms-code
Content-Type: application/json
```

请求：

```json
{
  "phone": "13800138000",
  "purpose": "login"
}
```

字段：

| 字段 | 类型 | 必填 | 说明 |
|---|---|---:|---|
| `phone` | string | 是 | 11 位手机号 |
| `purpose` | string | 否 | 当前仅支持 `login)，缺省按 `login) 处理 |

成功 `200`：

```json
{
  "data": {
    "expires_in": 600,
    "resend_after": 60
  },
  "request_id": "req_001"
}
```

开发期 `SMS_PROVIDER=mock` 时，验证码只输出到服务端日志，不在响应中返回。

业务规则：

- 同一手机号 60 秒内只能发送一次。
- 同一手机号每日最多发送 5 次。
- 应叠加 IP 限流，默认每日最多 10 次。
- 验证码有效期不超过 10 分钟，且只能使用一次。
- 日志中手机号必须脱敏，验证码不得写入普通业务响应。

常见错误：

| HTTP | code | message |
|---:|---|---|
| 400 | `INVALID_PHONE` | 手机号格式不正确 |
| 400 | `INVALID_PURPOSE` | 不支持的验证码用途 |
| 429 | `SMS_TOO_FREQUENT` | 验证码发送过于频繁，请稍后再试 |
| 429 | `SMS_DAILY_LIMIT` | 今日验证码发送次数已达上限 |
| 502 | `SMS_PROVIDER_ERROR` | 短信服务暂时不可用，请稍后再试 |

### 3.2 验证码登录/自动注册

```http
POST /api/v1/auth/login
Content-Type: application/json
```

请求：

```json
{
  "phone": "13800138000",
  "code": "123456"
}
```

字段：

| 字段 | 类型 | 必填 | 说明 |
|---|---|---:|---|
| `phone` | string | 是 | 发送验证码的手机号 |
| `code` | string | 是 | 6 位数字验证码 |

成功 `200)：手机号不存在时自动创建用户；已存在时直接登录。同时通过 `Set-Cookie` 下发刷新令牌。

```json
{
  "data": {
    "access_token": "eyJ...",
    "token_type": "Bearer",
    "expires_in": 1800,
    "user": {
      "id": 1,
      "phone_masked": "138****1234",
      "nickname": "用户1234",
      "created_at": "2026-08-30T19:00:00+08:00"
    }
  },
  "request_id": "req_002"
}
```

常见错误：

| HTTP | code | message |
|---:|---|---|
| 400 | `INVALID_PHONE` | 手机号格式不正确 |
| 400 | `INVALID_SMS_CODE` | 验证码错误或已过期 |
| 400 | `SMS_CODE_USED` | 验证码已使用，请重新获取 |
| 403 | `USER_DISABLED` | 当前账号不可用 |
| 500 | `AUTH_FAILED` | 登录失败，请稍后再试 |

### 3.3 刷新访问令牌

```http
POST /api/v1/auth/refresh
```

请求体为空，浏览器必须携带 refresh Cookie。

成功 `200)：刷新令牌轮换，旧令牌立即失效，并通过新的 `Set-Cookie` 下发刷新令牌。

```json
{
  "data": {
    "access_token": "eyJ...",
    "token_type": "Bearer",
    "expires_in": 1800
  },
  "request_id": "req_003"
}
```

错误：

| HTTP | code | message |
|---:|---|---|
| 401 | `REFRESH_TOKEN_MISSING` | 登录状态已失效，请重新登录 |
| 401 | `REFRESH_TOKEN_INVALID` | 登录状态已失效，请重新登录 |
| 401 | `REFRESH_TOKEN_REVOKED` | 登录状态已失效，请重新登录 |

### 3.4 登出

```http
POST /api/v1/auth/logout
```

请求体为空，浏览器必须携带 refresh Cookie。

成功 `200)：服务端吊销刷新令牌并清除 Cookie。

```json
{
  "data": {
    "logged_out": true
  },
  "request_id": "req_004"
}
```

即使 Cookie 已失效，也建议返回幂等成功并清除客户端登录态。

---

## 4. 问答接口

### 4.1 提交问题

```http
POST /api/v1/questions
Authorization: Bearer <access_token>
Content-Type: application/json
```

请求：

```json
{
  "content": "什么是进程和线程的区别？"
}
```

字段：

| 字段 | 类型 | 必填 | 说明 |
|---|---|---:|---|
| `content` | string | 是 | 1～2000 字符；服务端去除首尾空白后校验 |

成功 `200`：

```json
{
  "data": {
    "id": 1,
    "content": "什么是进程和线程的区别？",
    "status": "answered",
    "created_at": "2026-08-30T19:00:00+08:00",
    "answer": {
      "content": "## 进程与线程\n\n进程是……",
      "model": "deepseek-chat",
      "tokens_used": 832,
      "link_cards": [
        {
          "id": 1,
          "url": "https://www.bilibili.com/video/BVxxx",
          "title": "进程线程详解",
          "description": "进程与线程的基础概念讲解",
          "image_url": "https://cdn.qiniu.xxx/thumb/xxx.jpg",
          "media_type": "video",
          "position": 1,
          "site_name": "哔哩哔哩"
        },
        {
          "id": 2,
          "url": "https://developer.mozilla.org/zh-CN/docs/Glossary/Process",
          "title": "Process - MDN",
          "description": "进程相关概念说明",
          "image_url": null,
          "media_type": "link",
          "position": 2,
          "site_name": "MDN"
        }
      ]
    }
  },
  "request_id": "req_005"
}
```

简单问题无须参考资源时，`link_cards` 返回空数组：

```json
"link_cards": []
```

处理流程：

1. 校验 `content) 长度。
2. 本地敏感词过滤。
3. 腾讯云天御文本审核。
4. 创建 `pending) 问题并调用 LLM，超时 120 秒。
5. 审核 LLM 回答。
6. 提取回答中的 URL，最多处理 5 个，并抓取元数据。
7. 事务写入回答和链接卡片，状态改为 `answered)。
8. 返回完整结果。

状态与返回行为：

| status | 说明 | answer |
|---|---|---|
| `pending` | 处理中；同步接口通常不会返回此状态 | 可为空 |
| `answered` | 已完成并通过审核 | 完整回答 |
| `rejected` | 问题或回答未通过审核 | 不返回原始违规内容/回答 |
| `failed` | LLM 或必要流程失败 | 不返回空白回答 |

审核拒绝示例 `400)：

```json
{
  "error": {
    "code": "CONTENT_REJECTED",
    "message": "这个问题未通过内容审核，无法回答。"
  },
  "request_id": "req_006"
}
```

LLM 失败示例 `502)：

```json
{
  "error": {
    "code": "LLM_UNAVAILABLE",
    "message": "这个问题没有编译成功。网络或模型暂时不可用，请重试。"
  },
  "request_id": "req_007"
}
```

常见错误：

| HTTP | code | message |
|---:|---|---|
| 400 | `INVALID_CONTENT` | 问题不能为空 |
| 400 | `CONTENT_TOO_LONG` | 问题不能超过 2000 个字符 |
| 400 | `CONTENT_REJECTED` | 这个问题未通过内容审核，无法回答。 |
| 401 | `UNAUTHORIZED` | 请先登录 |
| 429 | `QUESTION_RATE_LIMITED` | 提问过于频繁，请稍后再试 |
| 502 | `LLM_UNAVAILABLE` | 这个问题没有编译成功。网络或模型暂时不可用，请重试。 |
| 502 | `MODERATION_UNAVAILABLE` | 内容审核服务暂时不可用，请稍后再试 |
| 500 | `QUESTION_SAVE_FAILED` | 问题保存失败，请稍后重试 |

### 4.2 查询历史问题

```http
GET /api/v1/questions?page=1&size=20
Authorization: Bearer <access_token>
```

查询参数：

| 参数 | 类型 | 必填 | 默认 | 约束 |
|---|---|---:|---:|---|
| `page` | integer | 否 | 1 | ≥ 1 |
| `size` | integer | 否 | 20 | 1～20，服务端限制最大值为 20 |

成功 `200)：

```json
{
  "data": {
    "items": [
      {
        "id": 1,
        "content": "什么是进程和线程的区别？",
        "status": "answered",
        "rejection_reason": null,
        "created_at": "2026-08-30T19:00:00+08:00"
      },
      {
        "id": 2,
        "content": "……",
        "status": "rejected",
        "rejection_reason": "policy",
        "created_at": "2026-08-29T18:20:00+08:00"
      }
    ],
    "pagination": {
      "page": 1,
      "size": 20,
      "total": 2,
      "total_pages": 1,
      "has_next": false
    }
  },
  "request_id": "req_008"
}
```

规则：

- 只查询当前登录用户的记录。
- 按 `created_at DESC, id DESC` 排序。
- 列表不得返回被拒绝回答的原文。
- 空列表时 `items) 为 `[])，不视为错误。

### 4.3 查询问答详情

```http
GET /api/v1/questions/1
Authorization: Bearer <access_token>
```

成功 `200)：返回与提交问题成功响应相同的问答对象；对于 `answered)，包含完整回答和 `link_cards)。

被拒问题示例：

```json
{
  "data": {
    "id": 2,
    "content": "……",
    "status": "rejected",
    "rejection_reason": "policy",
    "created_at": "2026-08-29T18:20:00+08:00",
    "answer": null
  },
  "request_id": "req_009"
}
```

失败问题示例：

```json
{
  "data": {
    "id": 3,
    "content": "如何理解 Go GMP 调度模型？",
    "status": "failed",
    "rejection_reason": null,
    "created_at": "2026-08-29T18:20:00+08:00",
    "answer": null
  },
  "request_id": "req_010"
}
```

错误：

| HTTP | code | message |
|---:|---|---|
| 401 | `UNAUTHORIZED` | 请先登录 |
| 404 | `QUESTION_NOT_FOUND` | 问题不存在 |
| 403 | `QUESTION_FORBIDDEN` | 无权访问该问题 |

为防止水平越权，推荐对不存在和不属于当前用户的记录统一返回 `404 QUESTION_NOT_FOUND)。

---

## 5. 媒体卡片数据约定

媒体卡片由问答接口内嵌返回，一期不单独提供媒体卡片 CRUD 接口。

| 字段 | 类型 | 可空 | 说明 |
|---|---|---:|---:|
| `id` | integer | 否 | 卡片 ID |
| `url` | string | 否 | 原始公网 URL |
| `title` | string | 是 | og:title 或页面标题 |
| `description` | string | 是 | og:description |
| `image_url` | string | 是 | 七牛 URL 或原图 URL |
| `media_type` | string | 否 | `video`、`image`、`link` |
| `position` | integer | 否 | 回答中出现的顺序 |
| `site_name` | string | 是 | 站点名称 |

判定规则：

- B 站、YouTube 等允许嵌入的视频站点：`video)。
- 图片直链（如 jpg/png/webp）或明确的图片资源：`image)。
- 其他 URL：`link)。
- 单条抓取失败时，不返回残缺卡片；可降级为 `link) 类型，仅保留可用 URL、标题或域名。
- 前端视频卡片点击后再加载 iframe；iframe 必须有可访问的 `title)。

---

## 6. 健康检查接口

### 6.1 存活检查

```http
GET /api/v1/health
```

成功 `200)：

```json
{
  "data": {
    "status": "ok"
  },
  "request_id": "req_health"
}
```

该接口只表示进程存活，不强依赖 MySQL、Redis 或外部服务。

### 6.2 就绪检查

```http
GET /api/v1/ready
```

就绪成功 `200)：

```json
{
  "data": {
    "status": "ready",
    "checks": {
      "mysql": "ok",
      "redis": "ok"
    }
  },
  "request_id": "req_ready"
}
```

未就绪 `503)：

```json
{
  "error": {
    "code": "SERVICE_NOT_READY",
    "message": "服务暂未就绪，请稍后再试"
  },
  "request_id": "req_ready_2"
}
```

`ready) 至少检查 MySQL 和 Redis 连接；LLM、SMS、天御、七牛是否作为就绪条件由部署配置决定，开发期 Mock 不应导致服务无法启动。

---

## 7. 错误码清单

| code | HTTP | 使用场景 |
|---|---:|---|
| `INVALID_PHONE` | 400 | 手机号格式错误 |
| `INVALID_PURPOSE` | 400 | 验证码用途不支持 |
| `INVALID_SMS_CODE` | 400 | 验证码错误或过期 |
| `SMS_CODE_USED` | 400 | 验证码已使用 |
| `SMS_TOO_FREQUENT` | 429 | 60 秒内重复发送 |
| `SMS_DAILY_LIMIT` | 429 | 达到每日发送上限 |
| `SMS_PROVIDER_ERROR` | 502 | 短信上游失败 |
| `REFRESH_TOKEN_MISSING` | 401 | 缺少刷新 Cookie |
| `REFRESH_TOKEN_INVALID` | 401 | 刷新令牌格式/签名无效 |
| `REFRESH_TOKEN_REVOKED` | 401 | 刷新令牌已吊销或过期 |
| `UNAUTHORIZED` | 401 | 缺少或无效访问令牌 |
| `USER_DISABLED` | 403 | 用户不可用 |
| `INVALID_CONTENT` | 400 | 问题为空 |
| `CONTENT_TOO_LONG` | 400 | 问题超过 2000 字符 |
| `CONTENT_REJECTED` | 400 | 问题或回答未通过审核 |
| `MODERATION_UNAVAILABLE` | 502 | 审核服务不可用且策略为拒绝 |
| `QUESTION_RATE_LIMITED` | 429 | 提问频率超限 |
| `LLM_UNAVAILABLE` | 502 | LLM 超时或调用失败 |
| `QUESTION_SAVE_FAILED` | 500 | 问答事务保存失败 |
| `QUESTION_NOT_FOUND` | 404 | 问题不存在或对当前用户不可见 |
| `QUESTION_FORBIDDEN` | 403 | 明确判定为越权 |
| `SERVICE_NOT_READY` | 503 | 服务依赖未就绪 |
| `INTERNAL_ERROR` | 500 | 未分类服务端错误 |

---

## 8. 联调验收清单

- [ ] 所有路径都带 `/api/v1) 前缀。
- [ ] 成功和失败响应都包含 `request_id)。
- [ ] 401 能触发前端刷新令牌，并且原请求最多重放一次。
- [ ] 4xx 不自动重试；5xx 最多重试 3 次。
- [ ] 登录成功返回访问令牌，并通过 httpOnly Cookie 下发刷新令牌。
- [ ] 验证码不能在响应中返回，Mock 验证码只写服务端日志。
- [ ] 违规提问不会调用 LLM，问题记录为 `rejected)。
- [ ] LLM 失败记录为 `failed)，客户端得到可重试提示。
- [ ] 简单问题的 `link_cards) 可以为空数组。
- [ ] 视频、图片和普通链接的 `media_type) 判定符合约定。
- [ ] 历史列表只返回当前用户数据，默认每页 20 条。
- [ ] 访问他人问题时返回 404 或 403，不能泄露内容。
- [ ] `/health) 和 `/ready) 可被 Docker/Nginx 探活。

