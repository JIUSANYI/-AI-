# 计算机知识 AI 问答平台 · UI 设计文档

| 文档属性 | 内容 |
|---|---|
| 项目名称 | cs-qa-platform |
| 文档版本 | v1.0 |
| 编写日期 | 2026-08-30 |
| 文档状态 | 待评审 |
| 关联文档 | 《需求分析文档》《设计方案》（同目录） |
| 适用范围 | 一期全部前端页面（Next.js App Router） |

---

## 1. 设计定位

**一句话定位**：一个让人想起"期末机房和自习室"的 AI 问答页——问题在终端里输入，答案像讲义一样读。

| 定位项 | 内容 |
|---|---|
| 用户 | 计算机在校生、在职开发者、计算机爱好者 |
| 页面唯一任务 | 让用户把问题问出去、把答案读明白 |
| 情绪目标 | 专注、可靠、有学科归属感（而不是"又一个通用 AI 聊天站"） |
| 质量底线 | 移动端可用、键盘可见焦点、尊重系统减弱动态偏好 |

**设计原则**：

1. **界面语言即学科语言**：配色取自代码语法高亮，输入取自终端，阅读取自讲义——不用与学科无关的装饰。
2. **一个大招，处处克制**：全站唯一的视觉爆点是 REPL 提问窗；其余部分安静、留白、纪律。
3. **阅读优先**：AI 回答是页面主体，排版服务 Markdown 长文阅读（行宽、行距、层级），不做花哨干扰。
4. **文案即设计**：按钮说"发送验证码"不说"提交"；错误说清楚发生了什么、怎么办。

**刻意避开的三个"AI 味"默认款**（设计前自检项）：

- ❌ 米白纸底 + 高对比衬线标题 + 陶土橙强调色
- ❌ 近黑底 + 单一荧光绿强调色
- ❌ Inter 字体 + 紫色渐变主按钮

---

## 2. 美学方向：讲义纸 × 终端（Lab Notebook × Terminal）

计算机学习有两个原生物质：**演算纸/讲义**与**终端**。本设计让二者分工——

- **问 = 终端**。提问框是一个"终端窗口"（macOS 圆点 + `❯` 提示符 + 方块光标），回车提交，回答像程序输出一样逐块浮现。
- **答 = 讲义**。AI 回答落在一张"讲义纸卡"上，用楷体阅读字体渲染，配代码块、参考资源卡片——像一份认真排版的课后讲义。

页面底纹是极淡的工程方格纸网格，暗示"演算与精确"。

---

## 3. 设计令牌（Design Tokens）

### 3.1 色彩 —— 「语法高亮」色系

命名直接借用代码编辑器语法角色，全站只有一套强调色体系：

| 令牌 | 色值 | 语法角色 | 用途 |
|---|---|---|---|
| `paper` 图纸白 | `#F7F9FA` | — | 页面底色（叠淡网格） |
| `ink` 制图墨 | `#1C2733` | 正文 | 主文字、标题 |
| `circuit` 电路蓝 | `#2A5CAA` | 关键字 | 主操作、链接、`❯` 提示符、焦点环、提问回显 |
| `pass` 运行绿 | `#1F7A4D` | 字符串 | 成功态、媒体卡片角标、次要强调 |
| `signal` 信号橙 | `#D9730D` | 数字 | 警示、警告、删除、高亮记号 |
| `comment` 注释灰 | `#8593A8` | 注释 | 次要文字、元信息、占位、禁用 |

派生色（由上表混出，不新增色相）：

| 令牌 | 色值 | 用途 |
|---|---|---|
| `grid-line` | `#E8EDF2` | 方格纸网格线（12px 见方） |
| `surface` | `#FFFFFF` | 卡片、输入框表面 |
| `circuit-wash` | `#EDF2FA` | 电路蓝 8% 洗底：选中态、代码块行号列 |
| `pass-wash` | `#EAF4EE` | 运行绿洗底：成功提示底色 |
| `signal-wash` | `#FBF0E5` | 信号橙洗底：警示提示底色 |
| `danger` | `#A32D2D` | 错误文字（红，仅用于错误） |

**用色纪律**：正文永远是 `ink`；一个视图内彩色强调不超过 2 处；错误红只出现在错误场景，不作装饰。**不做暗色主题**（列入二期）。

### 3.2 字体 —— 三个角色

| 角色 | 字体 | 级别 | 用途 |
|---|---|---|---|
| UI 无衬线 | Noto Sans SC（400/500） | 基础 | 导航、按钮、表单、列表、元信息 |
| 讲义阅读 | LXGW WenKai Screen（霞鹜文楷·屏显版，OFL 开源） | 特色 | **仅** AI 回答正文与标题——"读讲义"的体感 |
| 等宽 | JetBrains Mono（400/500） | 特色 | 提问输入、`❯`、代码块、logo、时间戳、数字 |

> 站位说明：文楷承担"讲义人格"，等宽承担"终端人格"，无衬线保证界面可用性——三角色各司其职，不混用。中文正文备选回退链：`"LXGW WenKai Screen", "Noto Sans SC", system-ui`。

### 3.3 字号阶梯（px）

| 令牌 | 值 | 用途 |
|---|---|---|
| `text-hero` | 26 / 500 | 提问页首屏标语（仅登录后首次，之后隐藏） |
| `text-title` | 20 / 500 | 讲义回答 H1、页面标题 |
| `text-heading` | 16 / 500 | 回答 H2、卡片标题、表单分组 |
| `text-body` | 15 / 400 | 讲义正文（阅读优化：行高 1.85） |
| `text-ui` | 14 / 400 | 按钮、输入、列表正文 |
| `text-caption` | 12 / 400 | 元信息、角标、时间戳（等宽字体） |

### 3.4 空间、圆角、边框

| 令牌 | 值 |
|---|---|
| 阅读列宽 | `max-width: 720px`（Markdown 长文最优行宽） |
| 间距阶 | 4 / 8 / 12 / 16 / 24 / 32 / 48 |
| 圆角 | 卡片 12px · 输入/按钮 8px · 角标 4px |
| 边框 | `1px solid #DCE3EA`（卡片）；`1px solid #2A5CAA`（焦点，替代阴影） |
| 阴影 | 一处都不用。层级靠底色差异 + 边框 + 间距表达（呼应终端的扁平质感） |

---

## 4. 布局系统与页面线框

全站统一骨架：**56px 顶栏 + 居中单列（720px）**。无侧边栏——一期只有 4 个页面，导航成本必须是一眼。

### 4.1 全局骨架

```
┌────────────────────────────────────────────────────┐
│ ◉ cs·qa          提问   历史                    (李) │ ← 56px 顶栏，等宽 logo
├────────────────────────────────────────────────────┤
│                    （方格纸底纹）                   │
│              ┌──── 720px 单列 ────┐                │
│              │    页面内容区       │                │
│              └────────────────────┘                │
└────────────────────────────────────────────────────┘
```

### 4.2 提问页（核心页，默认首页）

```
┌──────────────────────────────────────┐
│  ┌─ ● ● ● ───────────────── cs-qa ─┐ │ ← REPL 提问窗（签名元素）
│  │ ❯ 进程和线程的区别是什么？▌      │ │ ← 等宽输入，Enter 提交
│  └──────────────────────────────────┘ │
│         （提交后窗口收窄上移为顶栏下   │
│           的一条"回显行"）            │
│  ┌─ 讲义纸卡 ──────────────────────┐ │
│  │ 进程与线程          [楷体 H1]   │ │
│  │ 正文……（楷体，行高 1.85）       │ │
│  │ ┌────────────────────────────┐ │ │ ← 代码块：等宽+行号列
│  │ │ 1  pid = fork()            │ │ │
│  │ └────────────────────────────┘ │ │
│  │ ── 参考资源 ──                  │ │
│  │ ┌──────────┐ ┌──────────┐      │ │ ← 媒体卡片行
│  │ │ ▶ 视频卡 │ │ 图文卡   │      │ │
│  │ └──────────┘ └──────────┘      │ │
│  └────────────────────────────────┘ │
└──────────────────────────────────────┘
```

- 首次进入且未提问时，提问窗上方有一行 `text-hero` 标语："把问题交给终端。"（提问一次后永久隐藏，记住老用户）
- 提交后：提问窗内容固化为蓝色等宽"回显行"（`❯ 你的问题`），滚动锚点跳到讲义卡顶部。

### 4.3 登录页

```
┌──────────────────────────────────────┐
│            ◉ cs·qa                   │ ← 等宽 logo，放大居中
│         把问题交给终端                │
│  ┌─ ● ● ● ──────────────── cs-qa ─┐ │ ← 同款 REPL 窗（复用签名元素）
│  │ ❯ 手机号  [____________]       │ │
│  │ ❯ 验证码  [______] [发送验证码] │ │ ← 60s 倒计时按钮
│  │                                │ │
│  │         [ 登录 ⏎ ]             │ │ ← 主按钮：电路蓝底白字
│  └────────────────────────────────┘ │
└──────────────────────────────────────┘
```

登录卡本身就是一个小终端窗口——登录即"进入终端"，与提问页的隐喻连续。

### 4.4 历史页

```
┌──────────────────────────────────────┐
│  我的提问                    12 条    │
│  ┌────────────────────────────────┐ │
│  │ ❯ 进程和线程的区别是什么？      │ │ ← 等宽回显行
│  │   answered · 8月30日 19:02  →   │ │ ← 注释灰元信息
│  ├────────────────────────────────┤ │
│  │ ❌ 什么是 JSON（内容未通过审核）│ │ ← rejected 态：信号橙
│  │   rejected  · 8月29日 ……     →  │ │
│  └────────────────────────────────┘ │
│           [ 加载更多 ]               │
└──────────────────────────────────────┘
```

每行即一条"终端历史记录"，与其在 shell 里按上箭头翻旧命令的肌肉记忆一致。

### 4.5 详情页

与提问页提交后的状态一致：回显行（问题）+ 讲义纸卡（回答 + 媒体卡片）+ 底部"再问一个问题"按钮（点击回提问页并聚焦输入框）。

---

## 5. 签名元素：REPL 提问窗（全站唯一视觉爆点）

```
┌─ ● ● ● ──────────────────── cs-qa ─┐      窗口标题栏：三圆点（注释灰，
│                                    │      不用红黄绿 macOS 复刻，避免装饰化）
│  ❯ 输入你的问题，按 Enter 发送▌   │      ❯ = 电路蓝；placeholder = 注释灰
│                                    │
└────────────────────────────────────┘      光标：蓝底方块，600ms 闪烁
```

规格：

| 项 | 规格 |
|---|---|
| 结构 | 标题栏（三圆点 + 右侧 `cs-qa` 等宽小字）+ 输入区（单行 input，超高自动变 textarea） |
| 提示符 | `❯` 前缀固定渲染（不可删除），电路蓝 |
| 光标 | 自绘方块光标闪烁（`prefers-reduced-motion: reduce` 时静止） |
| 提交 | Enter 发送；Shift+Enter 换行；提交按钮 `发送 ⏎` 仅在移动端显示 |
| 长度 | 达到 2000 字符上限时右下角出现等宽计数 `1973/2000`，注释灰 → 信号橙 |
| 提交后 | 内容转为历史回显行（见 4.2），窗口清空回初始态 |

---

## 6. 组件规格

### 6.1 媒体卡片（LinkCard，三种形态）

| 形态 | 触发 | 视觉 | 交互 |
|---|---|---|---|
| `video` | B 站 / YouTube 等可嵌入站点 | 16:9 缩略图 + 居中圆形播放键（电路蓝 60% 透明底），底部标题 1 行 + 站点名角标 | 点击原地展开 iframe 页内播放，再点收起 |
| `image` | 图片直链 | 16:9 等比裁切展示 | 点击放大预览（一期可用原生 `dialog`） |
| `link` | 其余 URL | 左 60px 缩略图（无图时显示等宽首字母块），右侧标题 + 域名 | 整卡跳转新标签页 |

公共规格：白底、1px 边框、圆角 12px、宽度撑满阅读列（720px 内）；卡片组横向不超过 2 张（桌面）/1 张（移动），超出换行；角标用 `pass-wash` 底 + `pass` 字。抓取失败的 URL 降级为行内链接，**不渲染残缺卡片**。

### 6.2 讲义渲染器（MarkdownView）

| 元素 | 样式 |
|---|---|
| 正文 | 楷体 15px / 行高 1.85 / 段距 16px |
| H1 / H2 / H3 | 楷体 20 / 16 / 15，上边距 32 / 24 / 24 |
| 代码块 | 等宽 13px；`circuit-wash` 底；左侧 32px 行号列（注释灰）；水平滚动 |
| 行内代码 | 等宽 13px，`circuit-wash` 底，圆角 4px |
| 表格 | 表头 `comment` 底细线；单元格 12px 内边距；小屏横向滚动 |
| 列表 | 圆点用 `circuit`；嵌套缩进 24px |
| 引用块 | 左 3px `circuit` 竖线 + 左缩进 16px |
| 分隔线 | 1px `grid-line` |

### 6.3 按钮体系

| 类型 | 样式 |
|---|---|
| 主按钮 | `circuit` 底白字，8px 圆角，高 40px |
| 次按钮 | 白底 1px 边框 `ink` 字 |
| 危险操作 | `signal` 描边（一期仅"退出登录"） |
| 倒计时按钮 | 发送后变注释灰不可点，文字 `重新发送 58s`（等宽数字） |

### 6.4 Toast（轻提示）

底部居中，`ink` 底白字，圆角 8px，4s 自动消失；成功态左侧 4px `pass` 竖条，错误态 `signal`。不用系统 alert。

---

## 7. 状态设计（加载 / 空 / 错误）

所有状态文案遵守"说清楚发生了什么 + 下一步做什么"，不卖萌、不道歉。

| 场景 | 表现 | 文案 |
|---|---|---|
| 等待 AI | 讲义卡内骨架屏：楷体灰色 3 行 + 呼吸动画 | 顶部回显行下：`▌ 正在编译你的问题…`（等宽，蓝） |
| 链接抓取中 | 回答先展示，卡片位显示占位框 | 无需文案（占位框即可） |
| AI 失败 | 讲义卡变 `signal-wash` 底 | `这个问题没有编译成功。网络或模型暂时不可用，请重试。` + 「重试」按钮 |
| 内容被拒 | 状态标签 | `这个问题未通过内容审核，无法回答。` |
| 历史为空 | 空态插画位放一个大的 `❯_` 等宽字符（注释灰） | `还没有提问记录。回到终端，问出第一个问题。` + 「去提问」按钮 |
| 验证码错误 | 输入框下方 | `验证码不正确或已过期，请重新获取。` |

---

## 8. 动效规范

| 动效 | 参数 | 说明 |
|---|---|---|
| 光标闪烁 | 600ms step 交替 | REPL 灵魂，最显眼的动效，其他都为它让路 |
| 讲义浮现 | 回答区块 `opacity 0→1` + `translateY 8px→0`，300ms ease-out，逐块 80ms 阶梯 | "程序逐行输出"的体感 |
| 卡片悬浮 | `translateY(-2px)` + 边框加深，150ms | 仅媒体卡片有 hover 态 |
| 页面切换 | 无动画 | 直进直出，终端哲学 |

**全局规则**：所有动效包裹在 `@media (prefers-reduced-motion: no-preference)` 内；总时长不超过 300ms；不做滚动视差、不做装饰性循环动画。

---

## 9. 响应式与无障碍

### 9.1 断点（移动优先实现）

| 断点 | 布局 |
|---|---|
| < 640px | 单列；顶栏收缩为 logo + 头像；提问窗铺满；媒体卡片单列；代码块横向滚动 |
| ≥ 640px | 标准版式；媒体卡片可 2 列 |

### 9.2 无障碍底线

- 所有交互元素键盘可达；焦点环 `2px circuit`，清晰可见（不删 outline）
- 等宽字体字号不小于 12px；正文对比度 ≥ 4.5:1（`comment` 灰仅用于 ≥12px 非关键信息，对比度 3.9:1 场景仅限装饰性元信息）
- iframe 嵌入视频带 `title`；图片卡片有 `alt`（取抓取的 og:title）
- 表单错误用文字提示 + `aria-invalid`，不只用颜色区分

---

## 10. 文案规范（界面词汇表）

| 语境 | 用 | 不用 |
|---|---|---|
| 提交问题 | 发送 / 再问一个 | 提交、生成 |
| 登录 | 登录 ⏎ | 立即体验、马上开始 |
| 验证码 | 发送验证码 → 重新发送 58s | 获取 |
| 等待 | 正在编译你的问题… | AI 思考中…、思考ing |
| 失败 | 这个问题没有编译成功 | 抱歉、出错了、哎呀 |
| 列表 | 我的提问 | 个人中心、我的空间 |
| 名词 | 回答 | 响应、结果、输出物（"输出"仅限终端隐喻场景） |

语气：平主动语态、句首不大写混排、不加感叹号、不用表情符号。术语跟随用户：说"进程/线程/编译"，不说"任务/构建产物"。

---

## 11. 前端落地约定

| 项 | 约定 |
|---|---|
| 样式方案 | Tailwind CSS + CSS 变量双轨：3.1 色板定义为 `:root` CSS 变量，Tailwind 通过 `theme.extend.colors` 引用，业务类不写裸 hex |
| 字体加载 | 三个字体均自托管（next/font/local，避免外部 CDN 阻塞与备案合规问题）；文楷使用子集化 woff2（中文字体必须子集化，控制体积 < 2MB） |
| 字体回退 | `font-kai: "LXGW WenKai Screen", "Noto Sans SC", system-ui`；`font-mono: "JetBrains Mono", ui-monospace, monospace` |
| 网格底纹 | `background-image` 双 `linear-gradient` 画 12px 网格，颜色 `grid-line`，`background-attachment: fixed` 不用（移动端抖动） |
| 焦点环 | 全局 `:focus-visible { outline: 2px solid var(--circuit); outline-offset: 2px }` |
| 组件归属 | REPL 提问窗、LinkCard、MarkdownView 放 `src/components/`，均为客户端组件；讲义正文渲染放服务端（减少客户端 JS） |

---

## 12. 设计决策记录（含被否方案）

| 决策点 | 采用 | 被否方案 | 否决理由 |
|---|---|---|---|
| 主色 | 语法高亮色系（电路蓝主强调） | 紫色渐变 | AI 默认款；与学科无关 |
| 明暗 | 一期仅浅色 | 双主题 | 2C2G 前端预算内先做好一件事 |
| 氛围 | 工程方格纸网格 | 噪点纹理 / 渐变背景 | 渐变与噪点是装饰，网格是隐喻 |
| 提问框 | 终端窗口（REPL） | 大圆角聊天气泡 | 聊天气泡=又一个聊天站，无记忆点 |
| 回答字体 | 楷体讲义 | 全站无衬线 | 无衬线通篇=模板感；楷体给"读讲义"人格 |
| 圆点颜色 | 注释灰 | macOS 红黄绿复刻 | 复刻是装饰引用，灰色才克制 |
| 媒体卡片 | 静态卡+点击展开播放 | 直接嵌入自动加载 iframe | 省流量、首屏快，尊重不想看视频的用户 |
| 阴影 | 全站零阴影 | 卡片投影 | 扁平是终端质感的一部分；层级用边框表达 |

---

*本文档为 UI 实施的唯一视觉依据；实现阶段若需偏离，须先回本文档修订。*
