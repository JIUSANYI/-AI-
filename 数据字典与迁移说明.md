# cs-qa-platform 数据字典与迁移说明

## 1. 数据库基线

- MySQL 8.0、InnoDB、utf8mb4。
- 迁移文件按递增版本命名，例如 0001_init.sql。
- 启动时执行未完成迁移，并在 schema_migrations 记录版本。
- 一期不做分库分表，数据永久保留；用户只能访问自己的问题和回答。

## 2. 核心表

### users

| 字段 | 类型建议 | 约束 | 说明 |
|---|---|---|---|
| id | BIGINT UNSIGNED | PK | 内部用户 ID |
| phone | VARCHAR(20) | UNIQUE, NOT NULL | 手机号，日志中必须脱敏 |
| nickname | VARCHAR(64) | NOT NULL | 默认昵称 |
| status | VARCHAR(20) | NOT NULL | active/disabled |
| created_at、updated_at | DATETIME(3) | NOT NULL | 创建和更新时间 |

### refresh_tokens

| 字段 | 类型建议 | 约束 | 说明 |
|---|---|---|---|
| id | BIGINT UNSIGNED | PK | 记录 ID |
| user_id | BIGINT UNSIGNED | INDEX, NOT NULL | 所属用户 |
| token_hash | CHAR(64) | UNIQUE, NOT NULL | SHA-256 |
| expires_at | DATETIME(3) | NOT NULL | 过期时间 |
| revoked_at | DATETIME(3) | NULL | 吊销时间 |

### questions

| 字段 | 类型建议 | 约束 | 说明 |
|---|---|---|---|
| id | BIGINT UNSIGNED | PK | 问题 ID |
| user_id | BIGINT UNSIGNED | INDEX, NOT NULL | 所属用户 |
| content | TEXT | NOT NULL | 1～2000 字符 |
| status | VARCHAR(20) | NOT NULL | pending/answered/rejected/failed |
| rejection_reason | VARCHAR(64) | NULL | 拒绝类别 |
| created_at、updated_at | DATETIME(3) | NOT NULL | 时间字段 |

### answers

| 字段 | 类型建议 | 约束 | 说明 |
|---|---|---|---|
| id | BIGINT UNSIGNED | PK | 回答 ID |
| question_id | BIGINT UNSIGNED | UNIQUE, NOT NULL | 一对一关联问题 |
| content | MEDIUMTEXT | NOT NULL | 审核通过的 Markdown |
| model | VARCHAR(128) | NOT NULL | 模型名 |
| tokens_used、duration_ms | INT UNSIGNED | NULL | 用量和耗时 |

### link_cards

| 字段 | 类型建议 | 约束 | 说明 |
|---|---|---|---|
| id | BIGINT UNSIGNED | PK | 卡片 ID |
| answer_id | BIGINT UNSIGNED | INDEX, NOT NULL | 所属回答 |
| url | VARCHAR(2048) | NOT NULL | 已通过协议和 SSRF 校验 |
| title、description | VARCHAR/TEXT | NULL | 页面元数据 |
| image_url | VARCHAR(2048) | NULL | 七牛 URL 或原图 URL |
| media_type | VARCHAR(20) | NOT NULL | video/image/link |
| position | SMALLINT UNSIGNED | NOT NULL | 展示顺序 |

## 3. 关系与一致性

users 1:N questions 1:1 answers 1:N link_cards；users 1:N refresh_tokens。验证码不关联 MySQL 用户表，使用 Redis 按手机号保存验证码哈希和限流计数。

必须建立 questions(user_id, created_at, id)、link_cards(answer_id, position)、refresh_tokens(user_id, expires_at) 索引。Redis 验证码键 TTL 为 10 分钟，冷却键 TTL 为 60 秒；answers、link_cards 和问题状态更新必须在同一事务中完成。

## 4. 迁移流程

1. 新增版本化 SQL，不修改已执行迁移。
2. 在开发库检查字段、索引、字符集和回滚影响。
3. 通过后端启动流程执行并记录 schema_migrations。
4. 生产迁移前备份数据库，并采用向后兼容顺序。
5. 破坏性变更必须单独设计回滚或恢复方案。

迁移文件只负责结构变更，业务密钥不写入迁移。

说明：`0002_auth.sql` 曾创建 `sms_codes` 表，当前实现已迁移到 Redis，`0005_drop_sms_codes.sql` 会在升级时删除该遗留表。新环境执行完整迁移后不会保留验证码表。
