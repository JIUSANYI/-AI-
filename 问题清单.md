# 问题清单 — cs-qa-platform

- 生成时间：2026-09-07
- 依据：`代码审查报告.md` 的全量静态审查结论
- 状态：以下条目保留为审查历史；以各条目的当前状态和文末修复记录为准。2026-09-07 复核时，认证、重试幂等、迁移解析、缩略图退出、限流和前端请求超时等问题已修复；仍开放的问题见“待处理项”。

## 待处理项

- 生产依赖和外部服务尚未完成真实联调：腾讯云 SMS/TMS、LLM、七牛云、域名和 HTTPS。
- Next.js/PostCSS 依赖审计仍有高危项，需要单独升级并重新验证构建。
- SSRF DNS rebinding 窗口、审核拒绝内容展示、日报时区配置等低优先级改进尚未处理。
- CI 尚未执行完整浏览器端到端测试，也未演练备份恢复。

## 本清单的收录范围

仅收录**纯代码层面、本地开发环境即可复现与验证**的问题。以下内容按要求暂不列入：

| 暂不收录 | 举例 |
|---|---|
| 正式环境 / 部署相关 | HTTPS 与证书、nginx 监听与超时、TRUSTED_PROXIES 网段、CORS_ORIGINS 生产取值、容器内存限制、Redis 淘汰策略、docker 网桥网段 |
| 依赖第三方服务 | 腾讯云短信、腾讯云天御（TMS）内容安全、七牛云对象存储、LLM 供应商、证书签发 |

如需查看完整清单（含上述两类），见 `代码审查报告.md`。

---

## 高优先级 — 会导致功能错误或数据异常

### 1. 首页重试成功，但界面停留在失败状态（已修复，待整理）

- **位置**：`frontend/src/app/page.tsx:19`
- **现象**：`<QuestionAnswer question={question} />` 未传 `onUpdated`。而 `QuestionAnswer` 的重试逻辑是 `onUpdated?.(await api.retryQuestion(...))`，可选链会把结果静默丢弃。用户在首页提问 → 失败 → 点「重试」→ 后端成功生成答案 → **界面继续显示失败**，且答案已入库。
- **对比**：详情页 `questions/[id]/page.tsx:3` 传了 `onUpdated={(updated) => void mutate(updated, false)}`，行为正常。同一组件两条路径，一条能用一条不能用，且不报错。
- **建议**：首页改为 `<QuestionAnswer question={question} onUpdated={setQuestion} />`。更稳妥的做法是把 `onUpdated` 改为必填 prop，让漏传在编译期就报错。
- **当前状态**：已修复：首页现已传入 `onUpdated={setQuestion}`，重试成功后会立即更新答案视图。

### 2. 数据库迁移按分号切分 SQL，遇字符串字面量或触发器必炸

- **位置**：`backend/main.go:394-395`（`splitSQLStatements` 用 `strings.Split(script, ";")`）
- **现象**：这不是解析器，是一把剪刀。只要迁移脚本里出现字符串字面量中的分号、`TRIGGER`、`CREATE PROCEDURE` 或 `BEGIN...END` 块，SQL 就会被切成语法碎片并整体执行失败。目前没炸只是因为现有 5 个迁移恰好简单。
- **建议**：改用成熟的 SQL 解析（如 `github.com/huandu/go-sqlbuilder` 不适用，推荐 `pingcap/parser` 或为迁移脚本约定严格格式 + 使用 `mysql` 客户端多语句执行）；最低限度应跳过引号与注释内的分号。

### 3. 重试不接受「已回答」状态，网络抖动后无法恢复

- **位置**：`backend/questions.go:232`（`WHERE ... AND status='failed'`）、`:239`（返回 409 `QUESTION_NOT_RETRYABLE`）
- **现象**：请求实际上已成功（`status='answered'`、答案已入库），但前端因网络中断没收到响应。此时用户点重试会被 409 拒绝，只能手动刷新页面重拉。
- **建议**：retry 增加幂等分支——若 `status='answered'`，直接返回该问题的完整详情而不是报错。

### 4. logout 只撤销当前设备，其他会话仍然有效

- **位置**：`backend/auth.go:396`（`WHERE token_hash = ?`，仅撤销 cookie 中携带的那一个）
- **现象**：「退出登录」在用户心智中是退出全部设备，但实现只注销当前浏览器。多设备登录时其余会话不受影响。
- **建议**：改为按 `user_id` 批量撤销（`WHERE user_id = ? AND revoked_at IS NULL`）。若产品上确实需要保留其他设备，则应提供「退出所有设备」的独立入口，并在 UI 上说明当前按钮的作用范围。

---

## 中优先级 — 影响健壮性或可维护性

### 5. `answers.tokens_used` 永远是 NULL

- **位置**：`backend/questions.go:162`（INSERT 未包含该字段）、`backend/llm.go`（未解析响应中的 `usage`）
- **现象**：字段在 `0003_questions.sql` 建了，前端 `lib/types.ts:3` 也声明了，但后端从没写入过值，永远为 NULL。
- **建议**：二选一——在 `chatCompletionResponse` 中补 `Usage` 字段并在 INSERT 时写入；或从数据库、类型定义中移除该字段。不要留一个「存在但永远为空」的字段。

### 6. 前端提问请求无超时兜底

- **位置**：`frontend/src/lib/api.ts`（全文无 `AbortController`）
- **现象**：一次提问最长可能挂起 170s 以上，前端只能靠 `busy` 状态提示。若网络静默断开，用户会一直停在「正在编译你的问题…」。
- **建议**：为 `createQuestion` 与 `retryQuestion` 增加 `AbortController`（建议 180s），超时后提示「仍在处理中，请稍后查看历史」而不是直接判定失败。

### 7. 优雅退出不等待后台缩略图 goroutine

- **位置**：`backend/questions.go:268`（`go s.mirrorThumbnail(task)`）
- **现象**：进程收到退出信号后 10 秒内关闭，这些异步任务的数据库写入被直接丢弃，缩略图会永久停留在源站 URL。
- **建议**：用 `WaitGroup` 跟踪，在 shutdown 流程中带上限（如 5 秒）等待其完成。用户规模小时影响有限，但会留下难以排查的「偶发图片裂」。

### 8. SSRF 校验与实际拨号之间做了两次 DNS 解析，存在 rebinding 窗口

- **位置**：`backend/media.go:67`（`isPublicURL` 内 `net.LookupIP`）、`:219`（`publicDialContext` 内二次 `net.LookupIP`）
- **现象**：先解析校验、后独立解析拨号，两次之间攻击者可让域名改指向内网 IP。（`publicDialContext` 内部「解析一次并在同一结果上拨号」是安全的，问题出在前面那次额外解析。）
- **建议**：将解析结果传递到拨号阶段复用，或改用 `net.Dialer.Control` 在 connect 时校验对端 IP。

### 9. CSRF 依赖自造请求头，且 Origin 缺失时直接放行

- **位置**：`backend/auth.go:429`（`validCSRF`）
- **现象**：`X-CSRF-Protection: 1` 不是任何标准头；真正的防护来自 `SameSite=Lax` 与 CORS 白名单。而 `Origin` 为空时函数直接 `return true`，等于「没有来源即默认安全」。
- **建议**：保留现有头校验作为一层，但去掉「Origin 为空即放行」的分支——无 Origin 的非浏览器请求不应自动获得信任。

---

## 低优先级 — 清理与打磨

### 10. 被拒问题的内容被替换为「……」

- **位置**：`backend/questions.go:430-432`
- **现象**：问题未过审时，用户在历史与详情页都只能看到「……」，看不到自己写了什么，也没有任何说明。
- **建议**：至少给出一句解释（如「该内容未通过审核，已隐藏」）；若产品上允许，考虑保留原文仅隐藏答案。

### 11. 本地敏感词表仅 4 条，且匹配方式过于原始

- **位置**：`backend/moderation.go:111`（`strings.Contains`）
- **现象**：大小写敏感、无全半角与变体处理，加个空格或繁体即可绕过。
- **说明**：仅指本地兜底部分；与第三方审核服务的联动策略不在本清单范围。
- **建议**：匹配前做归一化（去空格、全角转半角、统一大小写）；词表扩充后考虑 Aho-Corasick 等多模匹配。

### 12. 链接卡片描述上限 16000 字符，明显过大

- **位置**：`backend/media.go:22`（`maxLinkDescriptionRunes = 16000`）
- **现象**：单条描述允许约 10 屏文字，一次回答 4 张卡最多可写入 64000 字符，最终渲染在前端的 `<small>` 里。
- **建议**：降到 300–500 字符量级，与卡片 UI 的实际展示能力匹配。

### 13. 硬编码东八区时区

- **位置**：`backend/auth.go:151`（`time.FixedZone("Asia/Shanghai", 8*60*60)`）
- **现象**：日报统计的「今日」边界写死在代码里，无法配置。
- **建议**：改为配置项（如 `DAILY_LIMIT_TIMEZONE`），默认 `Asia/Shanghai`。

### 14. `loadSensitiveWords` 复用 `err` 变量，容易误读

- **位置**：`backend/moderation.go:96`
- **现象**：`if err = scanner.Err(); err != nil` 会覆盖外层 `err`（当前逻辑正确，但阅读时需反向追踪作用域）。
- **建议**：改用独立变量名。

### 15. CSP 加在 JSON 接口上，实际不生效

- **位置**：`backend/main.go:208`
- **现象**：`default-src 'none'` 对返回 `application/json` 的接口没有实际约束作用，属于无害的装饰性配置。
- **建议**：保留无妨（成本为零），但不要把它计入「已完成 CSP 防护」。

---

## 本轮已修复（供对照）

以下 10 项已在 2026-09-05 修复，完整说明见 `代码审查报告.md` 的「修复记录」章节：

超时链路（nginx / WriteTimeout / `getQuestion` 独立 ctx）、登录并发注册竞态（`ON DUPLICATE KEY UPDATE`）、refresh_tokens 过期清理、Redis `volatile-lru`、前端容器 `USER node`、四处错误包装 `%w`、删除死表 `sms_codes`（`0005_drop_sms_codes.sql`）、`media.go` 去除多余拷贝、`X-Request-ID` 白名单校验、`accessLog` 改用 `defer`。

## 验证提醒

本机无 Go 工具链，以上结论未经 `go build` / `go test` 运行验证。修改后请在有 Go 的环境执行一次完整构建与测试。
