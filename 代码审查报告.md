# 代码审查报告 — cs-qa-platform

- 审查时间：2026-09-05
- 审查范围：`backend/`（16 个 Go 源文件，约 2900 行 + 4 个迁移）、`frontend/src/`（12 个源文件）、`deploy/`（3 个 compose + nginx）
- 审查方式：全量静态阅读 + 跨层配置链路核对
- **限制**：本机未安装 Go 工具链，`go vet` / `go test` 未能执行；所有结论基于代码阅读，未经运行时验证。

---

## 修复记录（2026-09-05 同日跟进）

以下条目已在本次审查后直接修复，均不涉及需要外部决策的信息：

| 条目 | 改动 |
|---|---|
| P0-2 超时链路 | `nginx.conf` `proxy_read_timeout` 180s → 240s；`main.go` `WriteTimeout` 180s → 240s；`questions.go` 的 `getQuestion` 改为独立的 3s 短超时 ctx（与 `markQuestionStatus` 保持一致），事务提交后不再因 ctx 耗尽而返回假 500 |
| P1-3 注册竞态 | `auth.go` 登录插入改为 `INSERT ... ON DUPLICATE KEY UPDATE id = LAST_INSERT_ID(id)`，并校验 `LastInsertId()`，并发首登不再返回 500 |
| P1-4 token 清理 | 新增 `cleanupExpiredRefreshTokens`（保留 7 天），`main.go` 在迁移完成后异步执行一次，删除已过期或已撤销超期的行 |
| P1-5 Redis 策略 | 生产与测试 compose 的 `--maxmemory-policy` 由 `allkeys-lru` 改为 `volatile-lru` |
| P1-7 容器权限 | `frontend/Dockerfile` 增加 `chown -R node:node /app` 与 `USER node`，与 backend 的 `USER app` 对齐 |
| P1-8 错误根因 | `moderation.go` 两处、`sms.go` 两处改为 `fmt.Errorf("...: %w", err)` 保留根因（`sms.go` 补充 `fmt` import） |
| P2-9 死表 | 新增 `migrations/0005_drop_sms_codes.sql` 删除从未被使用的 `sms_codes` 表 |
| P2-13 多余拷贝 | `media.go` 改为 `bytes.NewReader(body)`，去掉 1MB 的 string 与 reader 双重拷贝 |
| P2-14 请求 ID | `main.go` 新增 `sanitizeRequestID`，限制 64 字符并做 `[A-Za-z0-9_.-]` 白名单，非法值回退为服务端生成 |
| P2-15 访问日志 | `accessLog` 改用 `defer`，panic 时仍能记录状态码与耗时（详见该条目的说明更正） |

**未修复、待你确认外部信息**：

- **P0-1 HTTPS** — 需要你确认三件事：生产域名、证书来源（Let's Encrypt / 云厂商证书 / 外部 LB 终止），以及 TLS 终止位置。若终止在 nginx，需补 443 server 块与证书挂载；若终止在外部 LB，则本条可关闭。
- **P1-6 TRUSTED_PROXIES** — 需要你确认实际的 docker 网桥网段（`docker network inspect` 查看），`.env.prod.example` 里写死的 `172.16.0.0/12` 未必匹配你的环境。
- **P2-19 失败策略** — `MODERATION_FAILURE_MODE` 生产取 `reject` 意味着腾讯云 TMS 不可用即全站停摆，这是产品决策，需要你拍板。

**验证状态**：本机无 Go 工具链，`go vet` / `go test` 未执行。已人工复核全部改动片段的 import 使用与调用点，并确认现有 8 个测试文件不依赖被改动的 SQL 与请求 ID 行为（无 sqlmock 依赖）。**首次部署前请务必在 CI 或装好 Go 的机器上跑一次 `go build ./...` 与 `go test ./...`。**

---

## 总体评价

代码质量明显高于同规模项目的平均水平：错误处理完整、无裸 panic、SQL 全部参数化、SSRF 防护有实际实现（而非摆设）、密钥哈希存储、CSRF 与 CORS 配置有明确边界、测试文件齐备（8 个 `_test.go`，量级接近源码的 2/3）。安全基线的完成度是这份代码最强的部分。

问题集中在**部署配置层与超时链路**，而非业务逻辑。下面按严重度分级。

---

## P0 — 上线前必须修复

### 1. 生产无 HTTPS，但 refresh cookie 被强制标记 Secure → 登录态无法维持

- `backend/auth.go:71` — `cookieSecure: cfg.AppEnv == "prod"`
- `deploy/nginx/nginx.conf:5` — 只有 `listen 80;`，没有 `listen 443 ssl`
- `deploy/docker-compose.yml` — 映射了 `443:443`，但 nginx 配置里没有对应的 server 块，也没有证书挂载

`APP_ENV=prod` 时 `Set-Cookie` 会带 `Secure`，而浏览器**拒绝在非 HTTPS 连接下存储 Secure cookie**。后果链：

1. 登录接口返回成功，但 refresh_token 从未落盘；
2. access token 30 分钟后过期；
3. `AuthProvider` 启动时调用 `/auth/refresh`（`AuthContext.tsx:10`）拿不到 cookie → 401 → 判定未登录；
4. 用户表现为「登录后一刷新页面就掉线」，且无法自行恢复。

**修复**：给 nginx 补 443 + 证书（Let's Encrypt 或云厂商证书），并把 80 重定向到 443。若生产拓扑中 TLS 由外部负载均衡/CDN 终止，则需保证 `X-Forwarded-Proto: https` 被正确传递到后端——当前 nginx 已设置该头（`nginx.conf:15`），这种情况下本条可降级为「待确认」。

### 2. 超时链路层层相逼，长问题必然 504 或假失败

三方超时值互相卡死，且没有任何一层留足余量：

| 层 | 配置 | 值 |
|---|---|---|
| nginx | `proxy_read_timeout` (`nginx.conf:8`) | 180s |
| Go http.Server | `WriteTimeout` (`main.go:180`) | 180s |
| 提问请求 | `questionRequestTimeout` (`questions.go:16`) | 170s |
| LLM 调用 | `LLM_TIMEOUT` 默认 | 120s |

一次提问的最坏耗时路径是：内容审核(5s) → LLM(最多 120s) → 4 个链接并发抓取(5s) → 两次 DB 写入 → **再查一次库返回结果**（`questions.go:195`）。

两个具体故障：

- **504**：nginx 180s 与后端 180s 完全相等，没有任何传输余量。只要后端耗时逼近上限，nginx 先切断，前端收到 504 而非业务错误。
- **假失败（数据一致性问题）**：`answerQuestion` 全程复用同一个 170s ctx。若 LLM 耗时 168s，事务其实已成功提交（答案已入库），但最后的 `getQuestion` 立刻 `context deadline exceeded` → 返回 500。用户看到「提问失败，请稍后重试」，刷新历史页却能看见答案，且因为 status 已是 `answered`，重试按钮不出现、retry 接口返回 409。

**修复**：
1. nginx `proxy_read_timeout` 提到 ≥ 210s；
2. 后端 `WriteTimeout` 提到 ≥ 240s；
3. `questions.go:195` 的 `getQuestion` 改用自己的 `context.Background()` + 短超时（3s），与 `markQuestionStatus` 的做法保持一致——它已经这么做了（`questions.go:290`）。

---

## P1 — 应当修复

### 3. 新用户首次登录存在并发注册竞态

`auth.go:219-233`：先 `SELECT`，命中 `sql.ErrNoRows` 才 `INSERT`。两个并发请求（或用户连点两次发送+登录）会同时走到 INSERT 分支，靠 `users.phone UNIQUE` 兜底 → 第二个请求 INSERT 失败 → 直接返回 500「登录失败」。

**修复**：改用 `INSERT ... ON DUPLICATE KEY UPDATE id = LAST_INSERT_ID(id)` 后取 `LastInsertId()`，或捕获 MySQL 1062 后重新 SELECT。

### 4. refresh_tokens 无清理机制，且 logout 语义不完整

- 每次 refresh 都插入新行（`auth.go:324`、`auth.go:373`），TTL 30 天，无过期清理任务。多设备登录会持续累积。
- `logout`（`auth.go:390-393`）只撤销 cookie 中携带的那一个 token，其他设备的会话不受影响。「退出登录」在用户心智中是退出全部。

**修复**：启动时清理 `expires_at < NOW() - INTERVAL 7 DAY` 的行；如需「退出所有设备」，改为按 `user_id` 批量撤销。

### 5. Redis `allkeys-lru` 会淘汰验证码与限流计数

`deploy/docker-compose.yml` 与 `docker-compose.test.yml`：`redis-server --maxmemory 128mb --maxmemory-policy allkeys-lru`。

内存打满时 LRU 会淘汰**尚未过期**的 key，两类后果：

- `sms:login:*`（10 分钟 TTL）被淘汰 → 用户刚收到验证码就失效，且提示是误导性的「验证码错误或已过期」；
- `sms:daily:ip:*` 计数被淘汰 → **IP 维度限流被重置，短信轰炸防护失效**，直接后果是烧钱和被运营商模板封禁。

**修复**：改成 `volatile-lru`（只淘汰设有 TTL 的 key，本场景下行为相同但语义更安全），并加 Redis 内存告警。更根本的做法是让限流计数不被淘汰。

### 6. IP 维度限流强依赖 TRUSTED_PROXIES，配置漂移即失效

`auth.go:153` 用 `c.ClientIP()` 构造 `sms:daily:ip:*` 桶；`main.go:152` 调用 `router.SetTrustedProxies(cfg.TrustedProxies)`。

gin 的语义是：传入空数组会退化为信任 `0.0.0.0/0`（信任所有代理，取 XFF 最左值——**可被客户端伪造**）；而若 `TRUSTED_PROXIES` 配了网段但实际 docker 网桥不在其中（compose 默认可能用 10.x / 192.168.x，而 `.env.prod.example` 写死的是 `172.16.0.0/12`），`ClientIP()` 会返回 nginx 容器的 IP → **全站用户共用一个 IP 桶，10 次/天就把所有人的短信额度打满**。

**修复**：显式核对并写入实际网桥网段；建议在 `ready` 响应或启动日志中输出一次解析后的 ClientIP 便于上线验证。

### 7. 前端容器以 root 运行

`frontend/Dockerfile` 最终阶段无 `USER node`，而 backend 已正确设置 `USER app`。两边不一致。加一行 `USER node` 即可。

### 8. 四处错误信息丢弃根因，线上无法排障

`err` 被吞掉、只留固定文案：

- `moderation.go:143` — `errors.New("create Tencent TMS client")`
- `moderation.go:159` — `errors.New("Tencent TMS request failed")`
- `sms.go:80` — `errors.New("create Tencent SMS client")`
- `sms.go:97` — `errors.New("Tencent SMS request failed")`

腾讯云返回签名错误、余额不足、频率限制时，日志里只有一句固定话术，无法区分是配置问题还是供应商问题。

**修复**：改为 `fmt.Errorf("...: %w", err)`。若担心内部细节外泄，应在 handler 层统一脱敏，而不是在源头销毁证据。

---

## P2 — 建议改进

### 9. `sms_codes` 是死表
`migrations/0002_auth.sql` 建了 `sms_codes` 表，但代码全量走 Redis，从未 INSERT 或 SELECT。建议补一个 0005 迁移删除，避免误以为具备验证码审计能力。

### 10. `answers.tokens_used` 永远为 NULL
INSERT 语句未包含该字段，LLM 响应也未解析 `usage`。但 `frontend/src/lib/types.ts:3` 声明了这个字段。要么解析 `usage.completion_tokens` 写入，要么从类型定义中移除。

### 11. retry 的幂等性可以更友好
`answers.question_id` 是 UNIQUE（`0003_questions.sql`），所以重试不会插入重复答案——**这一点是正确的**。但 `retry` 只接受 `status='failed'`（`questions.go:230`）。若请求已成功但前端因网络中断未收到响应，用户只能手动刷新。建议对 `status='answered'` 直接返回已有详情（幂等），而非 409。

### 12. SSRF 防护存在 DNS rebinding 窗口
`media.go` 的 `isPublicURL`（:66）先做一次 DNS 解析校验，真正拨号时 `publicDialContext`（:218）又独立解析一次。两次解析之间，攻击者可让域名改指向内网 IP。实际风险中等偏低（只回读 HTML meta，且 `publicDialContext` 内部是「解析一次并在同一结果上拨号」，是安全的），但这是已知绕过模式。更稳妥的做法是用 `net.Dialer.Control` 在 connect 时校验对端 IP。

### 13. `media.go:172` 多一次 1MB 内存拷贝
`parseMetadata(strings.NewReader(string(body)))` — `string(body)` 与 `NewReader` 各拷贝一次。直接传 `bytes.NewReader(body)` 即可。

### 14. `X-Request-ID` 客户端完全可控且不校验长度
`main.go:246` 原样采信并回显到响应头与日志。建议限制长度（如 64 字符）并做字符白名单，避免日志注入与膨胀。

### 15. 中间件顺序导致 panic 请求丢失访问日志（已修复）

`main.go:156` 中 `gin.Recovery()` 位于 `accessLog()` 之前——这个**位置其实是对的**（gin 中先注册的在外层，Recovery 需要在外层才能捕获内层 panic）。真正的问题是 `accessLog` 把日志写在 `c.Next()` 之后且没有用 `defer`：panic 沿调用栈向上传播时，`c.Next()` 之后的语句不会执行，该请求的访问日志（含状态码）完全丢失。

**修复**：`accessLog` 改用 `defer` 记录，无论是否 panic 都能输出状态码与耗时。注意「把 Recovery 挪到最后」并不能解决问题，反而会改变错误恢复的层级——`defer` 才是正确解法。

### 16. 优雅退出不等待后台缩略图 goroutine
`questions.go:266` 用 `go s.mirrorThumbnail(task)` 异步转存缩略图。进程关闭时这些 DB 写入被直接丢弃，缩略图会永久停留在源站 URL——若源站有防盗链，图片就是裂的。10 人规模影响很小，但建议加 WaitGroup 并在 shutdown 时带上限等待。

### 17. 前端提问无超时兜底
`api.ts:19` 只对 GET 重试（POST 不重试，这是**正确**的设计，避免重复提问）。但一次提问可能挂起 170s+，前端没有 AbortController 超时，`QuestionForm` 只能靠 busy 态提示。建议加 180s 超时兜底并提示「仍在处理中，请稍后查看历史」。

### 18. 生产 `CORS_ORIGINS` 配错会导致全站 POST 403
同源的 POST 请求浏览器**也会**发送 `Origin` 头，而 `corsMiddleware`（`main.go:225`）未命中白名单直接返回 403。部署检查清单应把此项列为首项。

### 19. 敏感词表过薄，且失败策略偏激进
`sensitive_words.txt` 仅 4 条，`strings.Contains` 大小写敏感、无变体/全半角处理。作为兜底可以，重度依赖腾讯云 TMS。但需注意：`MODERATION_FAILURE_MODE=reject`（生产默认值）意味着 **TMS 不可用 = 全站无法提问**。建议评估生产是否改为 `allow`。

### 20. LLM 超长回答送审会失败
`llm.go:15` 允许最大 2MB 响应，而腾讯云 TMS 单次文本有长度上限（数千字符量级）。超长回答会导致审核接口报错 → `MODERATION_UNAVAILABLE` → 问题标记 failed。建议送审前截断（如前 5000 字符）或分段处理。

---

## 做对的地方（值得保持）

- **SQL 全参数化**，无字符串拼接注入面；
- **令牌哈希存储**：refresh token 与短信验证码均以 SHA-256 摘要入库/入 Redis，明文不落盘；
- **SSRF 防护有真实实现**：`isPublicURL` + `publicDialContext` + 重定向目标校验 + 响应体 1MB 限流，不是摆设；
- **限流分层**：60s 冷却 + 手机号 5 次/天 + IP 10 次/天，且用 Lua 脚本保证原子性；
- **失败回滚**：短信发送确定性失败时 `rollbackSMSReservation` 会释放冷却与配额，不会让用户白等 60 秒；
- **重试安全**：`answers.question_id` UNIQUE 从数据库层面杜绝了重复答案；
- **密钥校验前置**：`JWT_SECRET` 长度、CORS 禁止通配、TTL 正数校验，均在启动阶段失败退出，而非运行期暴露问题；
- **前端 XSS 面收敛**：`react-markdown` 未启用 `rehype-raw`，LLM 生成的 Markdown 无法注入 HTML；外链均有 `rel="noreferrer"`。

## 建议的修复顺序

1. P0-1（HTTPS）与 P0-2（超时链路）——这两个直接决定「能不能用」；
2. P1-5（Redis 淘汰策略）、P1-6（TRUSTED_PROXIES）——这两个决定「会不会烧钱 / 被刷」；
3. P1-3（注册竞态）、P1-4（token 清理）、P1-8（错误根因）——决定「线上好不好排查」；
4. 其余 P2 按迭代节奏安排。
