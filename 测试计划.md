# cs-qa-platform 一期测试计划

## 1. 测试目标

验证登录、问答、审核、资源卡片和历史记录的完整闭环，重点保证认证安全、用户数据隔离和外部依赖失败时的可控降级。

## 2. 核心测试用例

| 编号 | 场景 | 预期 |
|---|---|---|
| UT-01 | JWT 签发、过期、错误签名 | 正确校验，失败返回 401 |
| UT-02 | 验证码错误、过期、复用 | 拒绝并返回明确错误码 |
| UT-03 | 60 秒发送间隔和每日上限 | 返回 429，不调用短信上游 |
| UT-04 | 本地敏感词命中 | 记录 rejected，不调用 LLM |
| UT-05 | 云审核异常 | 默认拒绝，可按配置降级 |
| UT-06 | LLM 超时或 5xx | 记录 failed，返回可重试提示 |
| UT-07 | 回答审核拒绝 | 不展示原回答，记录拒绝类别 |
| UT-08 | media_type 判定 | video/image/link 分类正确 |
| UT-09 | SSRF 地址 | 环回、私网、云元数据地址被阻断 |
| UT-10 | 用户 A 访问用户 B 问题 | 返回 404 或 403，不泄露内容 |
| UT-11 | 链接抓取失败 | 不阻塞回答，降级为可用链接 |
| UT-12 | 事务失败 | 不产生半成品回答和卡片 |

## 3. 命令门禁

\`\`\`bash
cd backend
gofmt -d .
go test ./...
go vet ./...
govulncheck ./...

cd ../frontend
npm ci
npm run lint
npm run build
npm audit
\`\`\`

## 4. 浏览器冒烟

使用 Playwright CLI 验证：

功能与接口由本 Agent 验证；页面视觉、间距、色彩和交互一致性由 UI 协作模型依据《cs-qa-platform-UI设计文档.md》复核。

1. 登录页表单、验证码倒计时和错误提示。
2. Mock 验证码登录后进入提问页。
3. 提交问题时的等待态、Markdown 回答和资源卡片。
4. 历史列表、详情页和空状态。
5. 390px 移动布局、键盘焦点和 reduced-motion。
6. 失败时保存 screenshot/trace 到 output/playwright/。

## 5. 固定验收数据

- 普通概念问题：回答可以没有参考链接。
- 需要资料的问题：返回 1～4 个真实链接。
- 命中敏感词的问题：直接 rejected。
- LLM 模拟超时：failed 并显示重试提示。
- 伪造内网链接：抓取被 SSRF 防护阻断。
- 两个用户互相访问详情：必须被拒绝。

## 6. 发布门禁

- [ ] 单元、API 和浏览器冒烟测试通过
- [ ] git diff --check 通过
- [ ] 前端 lint/build 通过
- [ ] 依赖审计无未处理高危项
- [ ] Docker Compose 配置合法
- [ ] health/ready 正常
- [ ] 备份恢复流程已演练
