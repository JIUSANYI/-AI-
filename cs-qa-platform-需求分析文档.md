# 计算机知识 AI 问答平台 · 需求分析文档

| 文档属性 | 内容 |
|---|---|
| 项目名称 | cs-qa-platform（计算机知识 AI 问答平台） |
| 文档版本 | v1.0 |
| 编写日期 | 2026-08-30 |
| 文档状态 | 待评审 |
| 关联文档 | 《cs-qa-platform-设计方案.md》（架构与实现方案，已确认） |

> 修订记录
>
> | 版本 | 日期 | 说明 |
> |---|---|---|
> | v1.0 | 2026-08-30 | 依据需求沟通记录与已确认决策首次成文 |

---

## 1. 引言

### 1.1 编写目的

本文档对「计算机知识 AI 问答平台」一期建设的需求进行系统化分析与定义，作为后续设计、开发、测试与验收的基准依据。预期读者：项目所有者、开发实施方（AI 编程助手）、（未来的）测试与运维人员。

### 1.2 项目背景

计算机专业在校生与在职开发者在学习过程中，经常遇到概念理解、技术选型、错误排查等疑问，需要快速获得结构化、准确、易于理解的解答，并希望获得高质量的延伸学习资源（文档、教程、视频）。通用搜索引擎检索成本高、答案质量参差；通用对话 AI 缺少面向计算机学习场景的资源聚合能力。本项目构建一个专注计算机领域的 AI 问答平台，以"提问即答 + 按需附带富媒体学习资源"的形式解决该问题。

### 1.3 术语与缩略语

| 术语 | 定义 |
|---|---|
| LLM | Large Language Model，第三方大语言模型（一期通过 OpenAI 兼容协议 API 接入） |
| 链接卡片 | 对 AI 回答中出现的 URL 抓取元数据后生成的展示卡片（含标题、描述、缩略图，区分视频/图片/普通链接） |
| SMS | 短信服务（腾讯云短信） |
| 天御 | 腾讯云内容安全文本审核服务 |
| 七牛云 | 七牛云对象存储（Kodo），用于静态资源存储 |
| JWT | JSON Web Token，用于登录态保持 |
| 一期 / Phase 1 | 本文档定义的首个交付版本 |

---

## 2. 项目概述

### 2.1 项目目标

构建一个面向计算机学习者与从业者的在线 AI 问答网页平台，一期目标：

1. **G1 知识问答**：用户登录后可就计算机相关知识提问，平台调用第三方大模型生成 Markdown 格式的结构化回答。
2. **G2 富媒体资源展示**：当补充资源对理解确有帮助时，AI 回答附带真实参考链接；平台抓取链接元数据，以卡片形式在页面中展示视频与图片。
3. **G3 会话与历史管理**：用户可查看自己的历史提问与回答详情。
4. **G4 合规与安全**：对用户提问与 AI 回答进行内容审核，保障平台内容合规。

### 2.2 目标用户

| 用户群 | 描述 | 典型诉求 |
|---|---|---|
| 计算机在校生 | 大学计算机相关专业学生 | 概念理解（进程/线程、TCP 等）、作业与课程疑问、寻找学习视频 |
| 在职计算机从业者 | 程序员、运维、测试等岗位人员 | 技术选型对比、错误排查、官方文档定位 |
| 计算机爱好者 | 对计算机感兴趣的非从业者 | 入门引导、通俗解释、系统化学习路径 |

### 2.3 用户规模

一期按 **10 个注册用户** 量级进行设计与容量估算。**不设并发要求，不要求实时性**（问答为同步请求-响应模式）。

### 2.4 范围界定

**一期范围内（In Scope）**：

- 手机验证码登录/注册（自动注册）
- AI 问答（提问 → 审核 → AI 回答 → 落库）
- 回答中链接的元数据抓取与卡片展示（视频/图片/普通链接）
- 个人历史提问列表与详情
- 内容审核（提问与回答双向）

**一期范围外（Out of Scope，列为二期候选）**：

- **每日互联网新闻推送**（2026-08-30 变更评审后定级二期）：定时抓取行业信息源（新模型发布、框架更新等）→ AI 中文摘要 → 站内今日简报；需后端 cron 定时任务与新闻源管理；版权约束：仅存标题+原文链接+自有摘要，不转载正文
- **聊天室**（2026-08-30 变更评审后定级二期）：全站公共聊天大厅，用户间实时交流；需引入 WebSocket（推翻一期"不要求实时性"决策的技术演进点）；聊天消息为 UGC，需同步敏感词过滤 + 异步审核（先发后审、违规撤回）；需考虑最简管理员操作（删消息/禁言）
- 角色与权限划分（管理员后台、内容管理）
- 多轮对话 / 上下文连续追问
- 用户之间的社区互动（点赞、评论、分享、公开问答广场）
- 回答的流式输出（打字机效果）
- 搜索历史内容（按关键词检索自己的提问）
- 通知（短信/站内信）

> 二期决策记录：新闻推送与聊天室曾于 2026-08-30 提出一期加入，经评估（实时性技术引入、审核成本、一期交付聚焦）后确定**移入二期**。一期技术选型预留扩展空间：后端保持单服务架构，二期加 WebSocket 与 cron 时不需重构。

---

## 3. 用户场景（典型用例）

### UC-1 学习疑问求助（核心场景）

> 小李是计算机专业大二学生，晚上复习操作系统时对"进程和线程的区别"有疑惑。他打开平台，用手机号验证码登录后，在提问框输入问题。约十几秒后，页面展示一篇结构清晰的 Markdown 回答（定义、对比表、示例、小结）。回答末尾附带了 2 个参考链接：一篇官方文档和一个 B 站讲解视频，均以卡片形式展示，视频卡片可直接在页内播放。

### UC-2 工作中排查问题

> 在职后端工程师老张想了解 Go 的 GMP 调度模型。提问后，AI 给出深入的技术回答，并附带 Go 官方博客文章链接（普通链接卡片，含标题与缩略图）。老张点击卡片跳转原文继续阅读。

### UC-3 简单提问不带链接

> 用户问"什么是 JSON"。AI 判断该问题无需补充资源，给出简洁回答，末尾**不附带任何链接**，回答更快返回。此为正常预期行为，非缺陷。

### UC-4 查看历史提问

> 小李第二天想复习昨天的提问，进入"历史记录"页，按时间倒序浏览自己提过的问题，点击某条进入详情页，重新查看完整回答与媒体卡片。

### UC-5 内容被拦截

> 用户输入含违规内容的问题。平台在提问阶段即拒绝（提示内容不合规），不调用大模型，问题以 rejected 状态记录。

---

## 4. 功能需求

> 优先级定义：**P0** = 一期必须交付；**P1** = 一期尽量交付，可降级；**P2** = 二期。
> 编号规则：FR-{模块}-{序号}。

### 4.1 认证模块（AUTH）

| 编号 | 需求 | 优先级 | 验收标准 |
|---|---|---|---|
| FR-AUTH-1 | 用户输入手机号，请求短信验证码 | P0 | 60s 内可收到验证码（开发期为 Mock 模式，验证码输出到日志）；同一手机号 60s 内不可重复发送；每日发送量有上限（防刷） |
| FR-AUTH-2 | 用户提交手机号+验证码完成登录/注册 | P0 | 手机号不存在时自动注册并登录；已存在则直接登录；验证码错误/过期时返回明确错误码；验证码一次性使用 |
| FR-AUTH-3 | 登录态保持 | P0 | 访问令牌有效期 30 分钟；刷新令牌有效期 30 天且可吊销；用户可连续使用 30 天无需重复登录（除非主动登出） |
| FR-AUTH-4 | 登出 | P0 | 登出后刷新令牌立即失效，访问令牌在有效期内随请求失败被清除，用户回到登录页 |
| FR-AUTH-5 | 未登录访问受保护资源 | P0 | 返回 401，前端引导至登录页 |

### 4.2 问答模块（QA）

| 编号 | 需求 | 优先级 | 验收标准 |
|---|---|---|---|
| FR-QA-1 | 用户提交计算机相关问题 | P0 | 提问文本长度 1~2000 字符；空内容、超长均被拒绝并提示 |
| FR-QA-2 | 提问内容审核 | P0 | 提问先经本地敏感词过滤，再经云端文本审核；不合规则拒绝提问，返回明确提示，问题标记 rejected，不调用 LLM |
| FR-QA-3 | AI 生成回答 | P0 | 调用第三方大模型（OpenAI 兼容协议），以 Markdown 输出结构化回答（定义/对比/示例/小结等）；单次调用有超时控制（120s），失败时问题标记 failed 并向用户返回友好错误 |
| FR-QA-4 | 回答内容审核 | P0 | AI 回答落库前经过文本审核；不合规则标记 rejected，不向用户展示原文 |
| FR-QA-5 | 回答按需附带参考链接 | P0 | 仅当补充资源确有帮助时，AI 输出 0~4 个真实参考链接（官方文档、权威教程、视频等）；简单问题可为 0 个链接；**不得为必须附带** |
| FR-QA-6 | 提问历史列表 | P0 | 按时间倒序分页展示当前登录用户自己的提问（每页 20 条）；仅能看到自己的数据 |
| FR-QA-7 | 问答详情 | P0 | 展示问题原文、回答 Markdown 渲染结果、关联链接卡片；被拒/失败的问题展示对应状态提示 |

### 4.3 媒体卡片模块（MEDIA）

| 编号 | 需求 | 优先级 | 验收标准 |
|---|---|---|---|
| FR-MEDIA-1 | 链接元数据抓取 | P0 | 回答中含 URL 时，后端抓取 og:title / og:description / og:image 等元数据生成卡片；单链接抓取超时 5s，失败降级为纯链接文本，不阻塞回答展示 |
| FR-MEDIA-2 | 卡片类型区分 | P0 | 按目标特征判定 media_type：视频类（B 站、YouTube 等可嵌入站点）→ video；图片 → image；其余 → link。video 卡片在页内嵌入播放器，image 卡片展示图片 |
| FR-MEDIA-3 | 缩略图转存 | P1 | 抓取到的缩略图转存至七牛云；未配置七牛或转存失败时降级使用原图 URL；转存异步执行，不阻塞回答返回 |
| FR-MEDIA-4 | 抓取安全约束 | P0 | 仅抓取 http/https 公网链接；禁止请求内网/环回地址（SSRF 防护）；限制响应体大小与重定向次数 |

### 4.4 内容审核模块（MOD）

| 编号 | 需求 | 优先级 | 验收标准 |
|---|---|---|---|
| FR-MOD-1 | 本地敏感词过滤 | P0 | 作为第一层，命中敏感词直接拒绝，零 API 成本 |
| FR-MOD-2 | 云端文本审核 | P0 | 接入腾讯云天御文本审核，对用户提问与 AI 回答双向审核；审核服务异常时按配置决定放行或拒绝（默认拒绝，保守合规） |
| FR-MOD-3 | 审核结果留痕 | P0 | rejected 记录包含拒绝原因类别，便于排查误判 |

---

## 5. 非功能需求

### 5.1 性能

| 编号 | 需求 | 指标 |
|---|---|---|
| NFR-P-1 | 页面加载 | 前端页面首屏可交互 ≤ 3s（常规网络） |
| NFR-P-2 | 普通接口响应 | 登录、历史列表等非 LLM 接口 P95 ≤ 1s |
| NFR-P-3 | 问答接口响应 | 受 LLM 上游制约，不做硬性指标；目标 ≤ 60s（含 LLM 调用与链接抓取）；链接转存异步不阻塞 |

一期 10 用户规模，**无并发性能要求**。

### 5.2 可用性与运维

| 编号 | 需求 |
|---|---|
| NFR-O-1 | 提供 /health 与 /ready 健康检查端点，供容器与监控探活 |
| NFR-O-2 | 关键外部依赖（LLM/SMS/审核/七牛）密钥全部环境变量化，一处配置可切换厂商 |
| NFR-O-3 | 结构化日志（含请求 ID），问题可追溯 |

### 5.3 安全

| 编号 | 需求 |
|---|---|
| NFR-S-1 | 全站 HTTPS；访问令牌仅存内存，刷新令牌经 httpOnly Cookie 下发，不落 localStorage |
| NFR-S-2 | 刷新令牌在服务端哈希（SHA-256）存储，支持吊销 |
| NFR-S-3 | 验证码：60s 发送间隔、有效期 ≤ 10 分钟、一次性使用、每日每号发送上限 |
| NFR-S-4 | 链接抓取具备 SSRF 防护（见 FR-MEDIA-4） |
| NFR-S-5 | 用户只能访问自己的提问与回答（水平越权防护） |
| NFR-S-6 | 密钥不入代码库，.env 不提交版本控制 |

### 5.4 合规

| 编号 | 需求 |
|---|---|
| NFR-C-1 | 面向公众提供服务前完成 ICP 备案（部署于境内云服务器时） |
| NFR-C-2 | 用户提问与 AI 回答内容审核（见 FR-MOD） |

### 5.5 约束条件

| 类别 | 约束 |
|---|---|
| 技术栈 | 后端 Go（Gin）；前端 Next.js；数据库 MySQL 8；缓存 Redis 7 |
| 部署 | 腾讯云或阿里云单机 2核2G（Docker Compose，含 2G swap 兜底） |
| 静态资源 | 缩略图等静态资源存储于七牛云 |
| 外部服务 | 大模型走第三方 API（OpenAI 兼容协议，厂商可切换）；短信为腾讯云 SMS；审核为腾讯云天御 |
| 实时性 | 不要求；不引入 WebSocket/SSE |

### 5.6 兼容性

- 浏览器：Chrome / Edge / Safari / Firefox 最新两个大版本；移动端浏览器可用（响应式布局）。
- 不需要原生 App、小程序。

---

## 6. 数据需求（概念级）

核心数据对象与关系（详细表结构见设计方案文档）：

```
用户 User ──1:N── 提问 Question ──1:1── 回答 Answer ──1:N── 链接卡片 LinkCard
    │
    ├──1:N── 刷新令牌 RefreshToken（哈希存储）
    └──1:N── 验证码 SmsCode（一次性）
```

数据量估算（一期）：用户 ≤ 10，提问量按每用户每天 ≤ 20 条估算，日增 ≤ 200 条问答记录，单条回答（Markdown）≤ 64KB——MySQL 容量与性能均无压力，无需分库分表。

**数据保留策略**：一期所有数据永久保留（量极小）；用户删除账号功能不在一期范围。

---

## 7. 关键业务流程（需求视角）

### 7.1 提问流程

```
用户提交问题
   │
   ▼
[本地敏感词过滤] ──命中──> 拒绝，记录 rejected
   │通过
   ▼
[云端文本审核] ──不通过──> 拒绝，记录 rejected
   │通过
   ▼
[调用 LLM 生成回答] ──超时/失败──> 记录 failed，提示用户重试
   │成功
   ▼
[回答内容审核] ──不通过──> 记录 rejected
   │通过
   ▼
[提取 URL（如有）] ──有──> 抓取元数据生成卡片，缩略图异步转存七牛
   │（无 URL 则跳过）
   ▼
落库，向用户返回完整回答 + 卡片
```

### 7.2 登录流程

```
输入手机号 ──> 请求验证码（60s 限频）──> 收到短信
输入验证码 ──> 校验（一次性、有效期）
   ├── 未注册 ──> 自动创建账号 ──┐
   └── 已注册 ──────────────────┤
                                 ▼
              签发访问令牌（内存）+ 刷新令牌（httpOnly Cookie，服务端存哈希）
```

---

## 8. 验收标准（一期整体）

一期交付满足以下条件即视为验收通过：

1. **登录**：新手机号首次登录自动注册；连续使用中 30 天内无需重复登录；登出后立即失效。
2. **问答**：常规计算机问题获得结构化 Markdown 回答；简单问题可不带链接；带链接时卡片正确展示（视频可页内播放、图片正常显示、普通链接含标题缩略图）。
3. **审核**：违规提问在提交时被拦截；LLM 失败时用户收到友好错误而非空白页。
4. **历史**：可分页浏览自己的提问，进入详情完整还原回答与卡片。
5. **部署**：在 2核2G 单机上以 Docker Compose 一键拉起全部服务，内存峰值不超预算（约 900MB，swap 兜底）。
6. **安全**：越权访问他人提问返回 403/404；令牌不落 localStorage；密钥不出现在代码库。

---

## 9. 风险与开放事项

| 风险/事项 | 影响 | 对策（需求层面约定） |
|---|---|---|
| LLM 输出的链接可能是幻觉（不存在/错误） | 用户点击 404 | 抓取失败降级为纯链接；prompt 要求仅输出确定存在的权威资源 |
| 云服务账号未办理（SMS/天御/七牛） | 阻塞开发验证 | 全部外部服务均设开发期 Mock/降级模式，上线前切换真实凭证 |
| ICP 备案周期 | 上线时间 | 备案与开发并行推进 |
| 2G 内存紧张 | 服务 OOM | 各容器限内存 + 2G swap；见设计方案内存预算 |
| 公网暴露后验证码被刷 | 短信成本 | 发送限频 + 每日上限 + 图形验证码（P1） |

---

## 10. 需求确认清单

以下为已与需求方逐项确认的决策记录：

| # | 决策项 | 结论 |
|---|---|---|
| 1 | AI 模型来源 | 第三方大模型 API（OpenAI 兼容协议，厂商可切换） |
| 2 | 后端语言 | Go |
| 3 | 链接附带方式 | AI 回答中按需自动附带（可为 0 个） |
| 4 | 短信服务商 | 腾讯云 SMS |
| 5 | 内容审核 | 需要（本地敏感词 + 云端审核双层） |
| 6 | 架构方案 | 已确认（详见《设计方案》） |
| 7 | 界面要求 | 精美（应用 frontend-design 设计方法，已安装相应技能） |

---

*本文档与《cs-qa-platform-设计方案.md》配套使用；如有冲突，以最新确认版本为准。*
