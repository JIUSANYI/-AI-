# Browser requests use the same-origin /api/v1 path by default.
# Next.js proxies that path to this internal backend address.
BACKEND_INTERNAL_URL=http://localhost:8080

# Set only when the API must be called directly from the browser.
# NEXT_PUBLIC_API_BASE_URL=https://api.example.com/api/v1
