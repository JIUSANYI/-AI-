# cs-qa-platform 部署运维手册

## 1. 适用范围

本文档用于一期在 Ubuntu 24.04 LTS 单机服务器上的部署、更新、备份和故障处理。部署采用 Docker Compose，服务包括 nginx、frontend、backend、mysql 和 redis。

敏感信息（服务器密码、LLM Key、腾讯云凭证、七牛云凭证）只写入服务器受限权限的 .env，不写入 Git 或本手册。

## 2. 服务器基线

- 操作系统：Ubuntu 24.04 LTS
- 推荐资源：2 核、2 GiB 内存、至少 2 GiB swap、至少 20 GiB 可用磁盘
- 项目目录：/home/ubuntu/cs-qa-platform
- 公开端口：80、443；MySQL 和 Redis 不对公网开放
- 运行方式：Docker Compose

检查：

\`\`\`bash
free -h
df -h /
docker --version
docker compose version
\`\`\`

## 3. 首次部署

\`\`\`bash
cd /home/ubuntu/cs-qa-platform
git pull --ff-only origin main
cp backend/.env.example backend/.env
chmod 600 backend/.env
\`\`\`

编辑 backend/.env，生产环境至少确认 APP_ENV=prod、强随机 JWT_SECRET、正式 CORS_ORIGINS、SMS/审核/LLM/七牛配置和非默认数据库密码。

校验并启动：

\`\`\`bash
docker compose -f deploy/docker-compose.yml config
docker compose -f deploy/docker-compose.yml up -d --build
docker compose -f deploy/docker-compose.yml ps
\`\`\`

## 4. 发布、日志与排障

\`\`\`bash
cd /home/ubuntu/cs-qa-platform
git fetch origin
git checkout main
git pull --ff-only origin main
docker compose -f deploy/docker-compose.yml up -d --build
docker compose -f deploy/docker-compose.yml ps
curl -fsS http://127.0.0.1/health
curl -fsS http://127.0.0.1/ready
docker compose -f deploy/docker-compose.yml logs --tail=200 backend
docker stats --no-stream
\`\`\`

只部署已推送到 main 的提交。发生异常时保留日志和当前提交号，回滚到已知稳定提交并重新构建，不直接删除数据库卷。

## 5. 数据备份

每日备份 MySQL，备份文件放在权限为 700 的服务器目录并保留 7 份。备份脚本必须从受限环境变量读取密码，不得把密码写进脚本或 shell 历史。恢复前先停止 backend、确认目标数据库和备份文件，再执行恢复。

## 6. 上线安全检查

- [ ] 已修改初始服务器密码并配置 SSH 公钥
- [ ] SSH 仅允许必要用户，禁止 root 远程登录
- [ ] 防火墙只开放 22、80、443
- [ ] MySQL/Redis 仅监听 Compose 内部网络
- [ ] HTTPS 证书有效，生产 Cookie 开启 Secure
- [ ] CORS 为正式域名白名单
- [ ] .env 权限为 600，未被 Git 跟踪
- [ ] 已完成越权、SSRF、审核拒绝和 LLM 失败测试
- [ ] 已演练备份恢复

## 7. 回滚原则

应用版本回滚不等于数据库回滚。数据库迁移必须提供向后兼容策略或经确认的恢复方案，禁止使用 reset --hard 破坏服务器工作树。

