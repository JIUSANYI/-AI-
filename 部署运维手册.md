# cs-qa-platform 部署运维手册

## 1. 适用范围

本文档用于一期在 Ubuntu 24.04 LTS 单机服务器上的测试环境、正式环境部署、更新、备份和故障处理。两个环境使用独立目录、Compose project name、MySQL 数据卷、Redis 数据卷和环境变量文件。

敏感信息（服务器密码、LLM Key、腾讯云凭证、七牛云凭证）只写入服务器受限权限的 .env，不写入 Git 或本手册。

## 2. 服务器基线

- 操作系统：Ubuntu 24.04 LTS
- 推荐资源：2 核、2 GiB 内存、至少 2 GiB swap、至少 20 GiB 可用磁盘
- 测试目录：/home/ubuntu/cs-qa-platform-test
- 正式目录：/home/ubuntu/cs-qa-platform-prod
- 测试访问：8081（无域名时可用 `http://服务器IP:8081`）
- 正式访问：80、443；MySQL 和 Redis 不对公网开放
- 运行方式：Docker Compose

检查：

\`\`\`bash
free -h
df -h /
docker --version
docker compose version
\`\`\`

## 3. 首次部署双环境

\`\`\`bash
git clone --branch develop https://github.com/JIUSANYI/-AI-.git /home/ubuntu/cs-qa-platform-test
git clone --branch main https://github.com/JIUSANYI/-AI-.git /home/ubuntu/cs-qa-platform-prod
cp /home/ubuntu/cs-qa-platform-test/backend/.env.test.example /home/ubuntu/cs-qa-platform-test/backend/.env.test
cp /home/ubuntu/cs-qa-platform-prod/backend/.env.prod.example /home/ubuntu/cs-qa-platform-prod/backend/.env.prod
chmod 600 /home/ubuntu/cs-qa-platform-test/backend/.env.test /home/ubuntu/cs-qa-platform-prod/backend/.env.prod
\`\`\`

编辑两个环境文件。测试环境使用独立密码和 Mock 服务；正式环境必须使用强随机 JWT_SECRET、正式 CORS_ORIGINS、真实 SMS/审核/LLM/七牛配置和非默认数据库密码。两个环境不得复用数据库或 Redis 密钥。

测试环境校验并启动：

\`\`\`bash
cd /home/ubuntu/cs-qa-platform-test
docker compose --env-file backend/.env.test -f deploy/docker-compose.test.yml config
docker compose --env-file backend/.env.test -f deploy/docker-compose.test.yml up -d --build
docker compose --env-file backend/.env.test -f deploy/docker-compose.test.yml ps
\`\`\`

正式环境校验并启动：

```bash
docker compose --env-file backend/.env.prod -f deploy/docker-compose.yml config

docker compose --env-file backend/.env.prod -f deploy/docker-compose.yml up -d --build
```

## 4. 发布、日志与排障

日常发布通过 GitHub Actions：`develop` 推送到测试环境，`main` 推送到正式环境。正式环境绑定 GitHub `production` Environment，并要求人工审批。服务器上的手工发布必须明确指定对应目录和 Compose 文件。

\`\`\`bash
cd /home/ubuntu/cs-qa-platform-test
git fetch origin develop
git checkout develop
git pull --ff-only origin develop
docker compose --env-file backend/.env.test -f deploy/docker-compose.test.yml up -d --build
docker compose --env-file backend/.env.test -f deploy/docker-compose.test.yml ps
curl -fsS http://127.0.0.1:8081/api/v1/health
curl -fsS http://127.0.0.1:8081/api/v1/ready

cd /home/ubuntu/cs-qa-platform-prod
git fetch origin main
git checkout main
git pull --ff-only origin main
docker compose --env-file backend/.env.prod -f deploy/docker-compose.yml up -d --build
docker compose --env-file backend/.env.prod -f deploy/docker-compose.yml ps
curl -fsS http://127.0.0.1/api/v1/health
curl -fsS http://127.0.0.1/api/v1/ready
docker compose --env-file backend/.env.prod -f deploy/docker-compose.yml logs --tail=200 backend
docker stats --no-stream
\`\`\`

只部署已推送到 main 的提交。发生异常时保留日志和当前提交号，回滚到已知稳定提交并重新构建，不直接删除数据库卷。

## 5. 数据备份

每日备份 MySQL，备份文件放在权限为 700 的服务器目录并保留 7 份。备份脚本必须从受限环境变量读取密码，不得把密码写进脚本或 shell 历史。恢复前先停止 backend、确认目标数据库和备份文件，再执行恢复。

## 6. 上线安全检查

- [ ] 已修改初始服务器密码并配置 SSH 公钥
- [ ] SSH 仅允许必要用户，禁止 root 远程登录
- [ ] 防火墙只开放 22、80、443，以及受控的测试端口 8081
- [ ] MySQL/Redis 仅监听 Compose 内部网络
- [ ] HTTPS 证书有效，生产 Cookie 开启 Secure
- [ ] CORS 为正式域名白名单
- [ ] 测试和正式 env 文件权限为 600，且均未被 Git 跟踪
- [ ] 正式环境部署已配置 GitHub Environment 审批
- [ ] 已完成越权、SSRF、审核拒绝和 LLM 失败测试
- [ ] 已演练备份恢复

## 7. 回滚原则

应用版本回滚不等于数据库回滚。数据库迁移必须提供向后兼容策略或经确认的恢复方案。测试和正式环境分别回滚到各自分支上的稳定提交，禁止使用 `reset --hard` 破坏服务器工作树。
