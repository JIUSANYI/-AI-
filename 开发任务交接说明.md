# cs-qa-platform 开发任务交接说明

本文档用于交给负责落地开发的模型，目标是让其在不依赖聊天记录的情况下完成一期可运行、可验收的前后端实现。

## 1. 角色与协作边界

- 本 Agent（主负责人）：负责后端 Go、前端 Next.js 工程实现、接口联调、自动化测试、部署配置和最终合并。
- UI 协作模型：负责视觉设计、页面布局、交互细节、组件样式和视觉验收。所有视觉实现必须遵循《cs-qa-platform-UI设计文档.md》；不得引入新的视觉方向或组件库。
- 两方共享同一 Monorepo。任何接口字段、路由、状态、错误码或页面行为变化，先同步《接口文档.md》及相关设计文档。
- 当前明确不做：多轮上下文、流式输出、管理员后台、社区互动、通知、原生 App、小程序、WebSocket/SSE、消息队列、微服务、Kubernetes、GraphQL、BFF。

## 2. 必读资料与优先级

按以下顺序阅读：`欢迎.md`、`cs-qa-platform-需求分析文档.md`、`cs-qa-platform-设计方案.md`、`cs-qa-platform-技术栈文档.md`、`cs-qa-platform-UI设计文档.md`、`接口文档.md`、`agent.md`。冲突时以最新确认的用户决策、接口文档、设计方案、技术栈、UI 文档、Agent 指南为序。

## 3. 必须交付的功能

### 后端

1. Go 1.22+ / Gin 服务，统一 `/api/v1` JSON 响应、请求 ID、结构化日志、错误码和健康检查。
2. 手机号验证码登录/自动注册、60 秒限发、每日最多 5 次、一次性验证码；JWT 访问令牌 30 分钟，刷新令牌 30 天并通过 httpOnly Cookie 管理，支持轮换、吊销和 CSRF 防护。
3. 提问闭环：服务端校验 1～2000 字符 → 本地敏感词 → 云审核 → OpenAI 兼容 LLM（120 秒超时）→ 回答审核 → 事务落库。失败必须记录状态并给出可重试错误，禁止展示空白或未审核回答。
4. 回答中的 0～4 个真实 URL 资源卡片；仅处理公网 http/https，阻断环回、私网、云元数据和 DNS rebinding SSRF；抓取失败降级为纯链接。
5. 历史分页列表（默认 20 条）和仅限本人访问的详情接口；越权返回 403/404 且不泄露数据。
6. MySQL 版本化迁移（go:embed）与 Redis 验证码/限流/吊销；外部短信、审核、LLM、七牛服务均提供接口和 Mock 实现。

### 前端

1. Next.js 14 App Router + TypeScript strict，页面：`/login`、`/`、`/history`、`/questions/[id]`。
2. 集中实现 `lib/api.ts`：内存访问令牌、自动附加 Authorization、401 只刷新并重放一次、4xx 不重试、5xx 最多重试 3 次；刷新令牌只由 httpOnly Cookie 管理。
3. 按 UI 文档实现 REPL 提问窗、Markdown/GFM 回答、资源卡片、加载/错误/空状态、移动端布局、键盘焦点和 reduced-motion；不得写裸色值或使用外部字体 CDN。
4. 对所有表单做客户端提示，但以后端校验为准；不把令牌、验证码或敏感信息写入 localStorage、URL 或日志。

## 4. 实施顺序

按 M1→M6 逐步提交：M1 骨架与迁移；M2 认证；M3 LLM/审核/问答/历史；M4 URL 卡片与七牛；M5 四页面、API 客户端和 UI 联调；M6 天御、Compose、Nginx、备份与上线加固。每完成一个可运行的小步创建 Conventional Commit。

## 5. 质量门禁

提交前必须运行并修复结果：

```bash
cd backend && gofmt -w . && go test ./... && go vet ./...
cd ../frontend && npm ci && npm run lint && npm run build && npm audit
cd .. && docker compose -f deploy/docker-compose.dev.yml config
```

可用时追加 `govulncheck ./...` 和 Playwright CLI 冒烟（登录、提问、历史、详情、390px 移动布局、键盘焦点），失败产物写入 `output/playwright/`。提交前还要执行 `git diff --check`，检查完整 diff、失败路径、安全边界、文档联动和是否误改 UI 职责范围。

## 6. 交付报告格式

每次交付说明：改动文件；完成的业务规则；接口/数据结构变化；测试命令及结果；完整 diff 自审结论；未执行的检查及原因；已知风险和下一步。禁止提交密钥、真实手机号、验证码、令牌、调试输出或生成物。
